---
name: exe-packaging-deploy
description: "다른 PC 배포 = PyInstaller .exe(build.bat) → zip. 설정(구글시트 링크+네이버/OpenAI/구글SA 키) 자동 이식(무설정 설치). Chrome 필수"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4c9cd574-1676-4798-ba82-659441c10186
  modified: 2026-09-22T10:50:19.861Z
---

**터미널 없이 다른 PC 실행 = 더블클릭 .exe 배포**(2026-09-12 구축, 커밋 `87ec9bb`·`8ce6b07`, 실빌드·실행 검증됨).

- **빌드**: repo 루트 `build.bat` 더블클릭(또는 `python -m PyInstaller --noconfirm coupang_analytics.spec`) → `dist\쿠팡애널리틱스\쿠팡애널리틱스.exe`(+`_internal`). onedir·콘솔없음(console=False). 엔트리=`ui/app_qt.py`(PySide6).
- **spec 요점**: playwright `collect_all`(CDP용 node 드라이버 포함, 브라우저 다운로드 불필요), `coupang_analytics` 서브모듈 수집, **excludes 로 미사용 거대 패키지 제외**(tkinter·**torch**·scipy·pandas·matplotlib·botocore·boto3 등 — 환경에 깔려 딸려오던 것). 이걸로 **805MB→278MB**.
- **⭐경로/폴더 재설계(2026-09-21 — 개발·운용 동일·평평한 한 폴더, output·data·config=삭제금지, 소유자 지시):** 예전엔 배포 폴더 위치/이름이 노트북과 달라 재배포 시 상태를 못 찾음 → 구글시트 복원(오염·중복)·cold 프로필(차단)(2026-09-20 18:00 `C:\쿠팡애널리틱스` 실측). 소유자 최종 결정=**하위폴더 나누지 말고 노트북과 똑같은 평평한 구조**(코드+output+data+config.json 한 폴더)·**폴더명도 동일(`coupang-analytics`)**·상대경로·output·data·config=**삭제 금지(보존)**. 구현: `apppaths.data_root()`=base_dir(앱 폴더·frozen=exe폴더·개발=repo루트)·env `COUPANG_DATA_ROOT` override·`set_workdir` chdir·`output_dir()/data_dir()/config_path()`. 설정=`appconfig`(config.json, 비밀 아님만·키는 credstore), 시작 시 `_sync_config_and_registry`(config.json↔레지스트리, 파일 권위)·`_cfg_save_shared` 병행·`app.py._shared_setting`도 config.json 우선. spec COLLECT 폴더명=`coupang-analytics`(exe=쿠팡애널리틱스.exe). **build.bat=onedir 폴더째 zip + `_씨앗\`에 노트북 통계 마스터만 동봉**(⚠순위 프로필 data\chrome-pipeline=캐시 포함 947MB·신뢰쿠키 교차이전 제한이라 zip 미동봉·운용PC는 자기 data[휴지통 복원] 사용). install.ps1(평평·$root=$PSScriptRoot)=output/data 생성(있으면 보존)·config.json 생성(_설정값.json→있으면 보존)·**씨앗 마스터는 첫 설치 때만 output로 복사**(업데이트는 기존 보존·프로필 씨앗 있으면 data로도). **업데이트=새 zip 의 coupang-analytics\ 를 같은 자리에 덮어쓰기**(output·data·config.json 은 zip 에 없어 보존). ⚠교차 PC 신뢰쿠키(_abck) 이전은 device/IP 바운드라 제한적(머리만 warm·이후 야간에 스스로 warm). 검증(verify_offline·simulate[13·14 신규]·verify_gsheet) 통과. ⚠라이브 배포 확인 남음. CLAUDE.md 함정 9.
- **전제**: 실행 PC에 **Google Chrome 설치 필수**(정책=실제 Chrome+CDP). 없으면 크래시 대신 안내 대화상자 후 종료(`find_chrome` 체크). **API 키·비번은 DPAPI로 PC별 암호화**라 다른 PC에선 **재입력**(옮긴 값 복호화 불가).
- **설치 스크립트 1개로 통합(2026-09-20)**: 소유자 요청 — 다른 PC 배포 시 `설치.bat` **하나만** 실행. 야간무인+재부팅복구 예약 등록/해제를 `install.ps1` 4단계에 인라인(`Register-AutoTasks`/`Unregister-AutoTasks`, 프롬프트 **등록 Y / 해제 R / 건너뛰기 N**·우리 작업은 실행인자 --auto/--resume 로 탐지). **삭제**: `install_schedule.bat`·`install_schedule.ps1`·`uninstall_schedule.bat`·`uninstall_schedule.ps1`(로직 install.ps1 이관). **유지**: 소스배포용 `install_schedule_py.*`. build.bat 복사목록=설치.bat+install.ps1(+txt/md)만. install.ps1 은 UTF-8 BOM 필수(PS5.1 한글).
- **설치실행 파일(2026-09-17 추가)**: `deploy/설치.bat`(ASCII)+`deploy/설치.ps1`(**UTF-8 BOM**·한글). 다른 PC에서 폴더 복사 후 `설치.bat` 더블클릭 → ①쓰기가능 위치 점검(Program Files 경고) ②Chrome 확인(App Paths+기본경로) ③**바탕화면+시작메뉴 바로가기 생성**(WScript.Shell) ④(물어봄)야간 무인 자동실행 등록=`install_schedule.ps1` 호출(관리자권한 필요시 안내). `build.bat`이 설치.bat/.ps1·install/uninstall_schedule.*·INSTALL.md를 dist에 복사(기존 uninstall_schedule.ps1 미복사 버그도 수정). `uninstall_schedule.ps1` 필터를 exe도 매칭하게 확장(기존 python/app_qt만 → .exe+--auto/--resume 추가). ⚠ .ps1 한글은 반드시 BOM(PS5.1 cp949 깨짐 방지)=Write 후 `utf-8-sig` 재저장.
- **라이브 진단 exe(2026-09-20 추가)**: spec 이 이제 같은 onedir 에 **`쿠팡진단.exe`(console=True·읽기전용)** 도 함께 빌드(`MERGE`로 공유 의존성 중복 제거·엔트리=`tools/verify_login_discover_live.py`). 사무실 PC(exe만)에서 로그인→상품조회/수정(vid)+판매분석(지표)만 조회·콘솔 출력, **마스터/구글시트 미변경**. VID 출처 변경·판매상태(productStatus) 실동작을 앱 실행 전 안전 점검용. 입력 로딩은 앱과 동일(`_load_input`=QSettings input/source=gsheet면 관리대장 구글시트, 아니면 PC엑셀). `--list`·`계정ID`·`--manual` 인자. build.bat 이 `deploy/라이브검증_안내.txt`도 dist 에 복사. ⚠ `verify_login_discover_live.py`는 옛 3튜플 언팩 버그(판매상태 flag 4튜플 미반영)가 있었음—이번에 4튜플로 수정.
- **무설정 배포(2026-09-20 소유자 요구 — "압축 풀고 설치만"):** build.bat 이 이제 ④이 PC 설정을 exe `--export-settings`로 `dist\쿠팡애널리틱스\_설정값.json`(평문)에 담고 ⑤폴더를 `dist\쿠팡애널리틱스_배포.zip`으로 압축. 새 PC=zip 풀고 `설치.bat` 하나 → install.ps1 이 바로가기·야간예약 + **exe `--import-settings`로 설정 자동 적용**(QSettings 구글시트 링크·입력소스 + credstore `__naver__`/`__openai__`/`__gsheet_sa__`를 그 PC용 DPAPI 재암호화)+**평문 _설정값.json 자동 삭제**. **설정 탭 재입력 불필요**(소유자 확정=진짜 무설정, 키 3개 포함). 계정 비번은 여전히 관리대장에서 자동. 구현=`ui/app_qt._export_settings`/`_import_settings`(main 최상단 인자 처리·GUI 안 뜸·print는 cp949 안전문자)·`Start-Process -Wait`로 헤드리스 실행 대기. ⚠**보안**: _설정값.json·배포 zip 에 API/SA 키 **평문** → 외부 공유 금지·설치 후 삭제. `.gitignore`에 `_설정값.json` 추가(커밋 차단). 이전 "키는 PC별 재입력"은 이 방식으로 대체(수동 입력은 자동적용 실패 시 폴백).
- **⭐Windows Defender 오탐 삭제 대응(2026-09-22 실측·해결):** 미서명 PyInstaller exe 는 Defender ML 이 **오탐**(Wacatac/Wacapew `!ml`·실제 바이러스 아님)으로 **자동 격리·삭제**함(다른 PC 설치 시 `쿠팡애널리틱스.exe` 삭제 실측·스크린샷). 해결=**Defender 검사 제외**: (a) `deploy/0_먼저실행_보안제외.bat`(관리자·**압축 풀기 전** 실행 → `Add-MpPreference -ExclusionPath C:\coupang-analytics` + bat 위치 옆 폴더). **build.bat 이 zip 루트+폴더에 항상 동봉**(`Compress-Archive -Path dist\coupang-analytics,dist\0_먼저실행_보안제외.bat`). (b) `install.ps1` 0.4단계=설치 시 `$root` 를 `Add-MpPreference -ExclusionPath`(설치 후 재검사 재격리 방지·try/catch). (c) `첫실행_설정안내.txt` 0)단계=보안제외 **먼저**. ⚠제외는 **반드시 압축 전에**(exe 는 푸는 순간 삭제). UPX 미사용(오탐 요인 아님). 근본해결=코드서명(비용·사내 소수 PC엔 과함). CLAUDE.md 함정 10.
- **자동실행 삭제 파일(2026-09-20 소유자 요구):** `deploy/자동실행_삭제.bat`(ASCII 런처)+`deploy/remove_autorun.ps1`(UTF-8 BOM). 야간무인(--auto 18:00)·재부팅복구(--resume 로그온) 스케줄 작업을 **실행인자 매칭(--auto/--resume)** 으로 찾아 `Unregister-ScheduledTask`(exe·python소스 둘 다·한글 작업명 회피). **⚠삭제=관리자 권한 필요**(작업이 관리자로 등록됨) → bat 우클릭 "관리자 권한으로 실행"(실패 시 스크립트가 안내). build.bat 이 패키지에 복사. 다시 켜기=`설치.bat` 등록(Y). **소유자 PC(dev)에서 2작업 삭제 실행 완료(관리자 승격 승인·(없음) 확인).** 소스 스케줄러=`install_schedule_py.*`(유지).
- 안내서 = repo `INSTALL.md`(빌드/설치 절차·설치.bat 흐름), 배포본 `첫실행_설정안내.txt`(무설정 흐름·보안)·`라이브검증_안내.txt`(VID 검증 절차). 관련 [[runtime-ui-and-always-on]] [[login-policy-real-browser-only]] [[feature-vid-source-from-product-list]].
