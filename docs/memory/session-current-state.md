---
name: session-current-state
description: ⭐현재상태(2026-09-20) — VID출처 변경 1~7단계 구현 완료·오프라인 검증 통과·라이브 배포 준비 완료(쿠팡진단.exe 읽기전용·빌드검증). 다음=사무실서 build.bat→dist복사→쿠팡진단.exe 확인→마스터 백업 후 앱 ①실행
metadata:
  node_type: memory
  type: project
  originSessionId: 3a577ce1-852a-4bb4-b64f-e6d46ce615f6
  modified: 2026-09-20T07:19:05.579Z
---

**⭐2026-09-20-11 (후속 5건 + 키워드=시트값 + 작업전 백업·커밋 ac12649·53174ef·7f6511d): 옵션분리 위 마무리.** 이 세션 4커밋(모두 로컬 master·미푸시). ①**옵션분리 후속 5건**(ac12649): 계정목록=상품별 1줄(`_is_secondary_option`+`_product_rows` 필터·2차옵션 제외)·동결키워드 검색량 네이버채움(`keyword_search`+`_fill_frozen_search_volumes`)·**마스터없으면 gsheet복원**(`restore_master_from_gsheet`, UI가 master_exists 판정 전 호출)·변형상품 서식 그룹화(apply_style 같은 등록명 그룹 바깥만 굵은선)·실행모드 용어("오늘 것만 다시 수집"·"통계 전체 초기화(백업 후)"). ②**키워드=구글시트 셀 값 기준**(53174ef, 소유자 최종확정·이력보존 폐기): 시트에 채워진 키워드=추적대상·변경체크 없이 그대로·삭제=`clear_keyword_row`(과거값까지 삭제)·**AI 톱업 없음**(4개 중 2개만 있으면 2개만·빈칸 공란)·새상품(0개)만 AI·상한 `KW_MAX_TRACK=10`(가변)·안전장치(시트 0개면 미변경). 중간에 넣었던 중단/이력보존(69e7561)은 되돌림. ③**작업 전 자동 백업**(7f6511d): `backup_sources`가 모든 작업 시작 시 로컬 마스터+결과시트+관리대장을 `output/백업/`에 타임스탬프 저장(UI 전 진입점). ④정밀시뮬 stale 수정(simulate_stages `_rows`가 4행패딩 빈행을 키워드로 오인→제외, 18/0 통과). **실측: 실제 구글시트에 담당자 키워드 편집 2건 대기**(휴라엘 거북목교정기·캠핑타프)→다음 18시 실행이 반영(옛 키워드·이력 삭제). SA 편집권한 2시트 확인·구글시트 사본 백업 완료. 검증 4종 통과. SSOT=DESIGN §0-000000·§0-0000000, [[daily-stats-keyword-freeze]] [[pending-code-fixes-next-session]].

**⭐2026-09-20-10 (구글시트 반영 안 됨 진단·로그·커밋 1c537ed): 원인=①미완료.** 소유자: 다른 PC 정상종료인데 구글시트 반영 안 됨(전날도)·로그도 없음. **진단(09-19 18:01 run_log)**: 26계정 중 2계정만 처리 후 **3번째 로그인('확인 중')에서 로그 끊김=막힘** → ①이 끝까지 완료 못 함 → `_push_gsheet`(run_full 맨 끝 1회)에 **도달 못 해 반영 0**. 이 PC는 출력URL·SA키 정상 설정(→앱 배선 정상). **구조적**: gsheet push는 ① 계정 루프 완료 후에만 실행. **조치**: ①2차인증 건너뛰기(-9)로 막힘 해소 ②`_push_gsheet` **조용한 스킵 제거**=빈 URL/SA미등록/단계별 진행/실패단계(_phase)+예외 **항상 로그**(기존 빈 URL이면 무로그였음). 로그 없으면 "① 미완료"로 판단 가능. ⚠남은 개선여지: ①이 crash/kill로 중단되면 여전히 push 못 함(중간 반영 없음) — 필요 시 주기적/finally push 검토.

**⭐2026-09-20-9 (2차인증 건너뜀·커밋 3280b05): 배치서 2차인증=대기없이 다음 계정.** 2차 인증(인증번호) 화면 뜨면 타임아웃 대기 없이 즉시 그 계정 건너뛰고 다음 진행(미완료→다음 실행 재시도)·**로그 남김**(브라우저 감지 + 계정명 단위 2곳). `wait_for_login(skip_on_otp=True)`·`_login_and_discover`(otp_seen 가드로 반자동 재시도도 건너뜀). **구글시트 반영 시점(현행)**: 각 단계(①run_full·②select_keywords_stage·③track_ranks_stage) **완료 시 1회씩** `_push_gsheet`(마스터 저장 직후·통계 전체미러+계정목록 동기화). 그로스재고 역기록은 전체실행/무인 종료 시. 계정마다가 아니라 단계 완료 시(2차인증 건너뜀으로 단계가 빨리 끝나 반영도 빨라짐).

**⭐2026-09-20-8 (미매칭 해결·커밋 dd584e4·4f7163c): 대장 중복정책 + 규격 타이브레이커.** ①**대장 중복 상품 정책**=한 계정에 같은 상품명 2줄+=담당자 오입력→첫 줄만 추적(`_parse_grid` dedup·공백정리키·경고로그, 파일/구글시트 공용, verify_offline[13]). ②**규격 타이브레이커**(`product_match._assign`+`_spec_tokens`)=핵심어 동점(변형)일 때 대장 규격토큰(120정/30포)과 겹치는 후보 정확히 1개면 vid 확정(핵심 정밀도 불변·못 가르면 미매칭). **라이브 결과: wellbing1107 14/17→16/16 전건 매칭**(알부민=중복제거·퀘르세틴 120정/30포·루바브 120정 각각 정확 vid 확정). 검증 verify_offline[11·13]·simulate·gsheet 통과.

**⭐2026-09-20-7 (Step7 재적용·미매칭 분석·커밋 871706a): 판매상태=productStatus 확정.** 다계정 실측으로 6의 되돌림을 정정 — productStatus 는 정상 변동(nicoable ON_SALE4/PARTIAL1/SUSPENDED2·sg0141n ON_SALE6/PARTIAL3/SUSPENDED4)하고 **화면 판매/승인상태와 일치**. enum=ON_SALE→판매중·PARTIAL_ON_SALE→부분판매중·SUSPENDED→판매중지·DRAFT→임시저장·REJECTED→승인반려. wellbing1107 은 '신규 등록 불가' 제한계정이라 전부 SUSPENDED 였을 뿐(필드 정상). → **판매상태 소스=productStatus(전 상품·판매자배송 커버)·RFM 폴백** 재적용. **⭐미매칭 3건 원인 규명(wellbing1107)**: ①알부민=**대장에 동일이름 2줄 중복**(쿠팡 1개→1줄만 매칭·나머지 미매칭, **대장 중복삭제 필요**) ②퀘르세틴·③루바브=**핵심어 포함 발견상품 2개 동점**(규격 30포/120정은 _SPEC로 매칭토큰 제외→변형 구분 불가→마진0→애매 미매칭=오매칭방지 안전동작). 교훈=한 계정만 보고 판단 금지([[fix-from-real-evidence]]).

**⭐2026-09-20-6 (라이브 검증·Step7 되돌림→재적용): 사무실 실로그인 검증 완료.** wellbing1107 1건 로그인(2차인증 없이 통과)→상품조회/수정+판매분석 실측(verify_login_discover_live). **결과: vid 출처 변경 정상 동작** — 상품조회 51리스팅→47상품·둘다 NORMAL 73옵션 제외·대장 17→매칭14·vid80·미매칭3(등록명 표시)·옵션분리(맥문동환 6옵션 등)·판매분석 실지표(노출/판매/방문)·RFM재고 52. **⚠중대 발견: 상품조회 `productStatus`는 판매상태 소스로 못 씀** — 전 옵션 320개 SUSPENDED **상수**(재고 있는 활성 상품도 SUSPENDED), `valid`도 320개 전부 INVALID 상수. RFM `isSaleSuspended`는 판매중지42·판매중10 정상 구분. → **7단계(productStatus 판매상태) 되돌림**, 판매상태 소스=RFM 유지(판매자배송 미상 한계 원래대로). productStatus/valid=관측 로그로만. **INVALID 의미**: 폐기 아님(displayDeletedProduct=false로 삭제분 제외됨)·이 계정 전 옵션이 상수라 의미 판별 불가. [[feature-sale-status-mismatch-flag]] 갱신.

**⭐2026-09-20-5 (표준 로그 포맷·커밋 c6df0e4): 전 소스 통일.** 소유자: 로그에 일자·시분초 없고 형식 제각각→분석 불가. **표준=`[YYYY-MM-DD HH:MM:SS.mmm] 메시지`**(정렬·grep·소요시간 분석). SSOT=`config.log_ts()`/`config.format_log(msg)`. 적용=app_qt(_append_log·기존 1/60프레임→밀리초)·app.py·진단/라이브 도구(_log)·저수준 진단 print(browser/rank/session_state, browser.py에 config import 추가). 메시지 본문 규칙(별도)=상품/옵션은 `vid=` 태그. 검증 3종 통과.

**⭐2026-09-20-4 (로그 정리·커밋 24dd645): VID=오류·진행 추적 키.** 소유자: 현재 로그는 상품명 기준이라 vid 추적 불가·`_fill_product_metrics`가 로그 무기록이라 분석 불가. → 모든 상품/옵션 단위 로그에 **`vid=` 태그**(grep 키). `_vtag`/`_short` 헬퍼. `[지표] vid=… 이름 [구분] 노출·판매·방문·재고`(지표없는vid·재고누락 명시)·`[계정 id/biz] 소스별 개수+미매칭 등록명 목록`·키워드/순위/노출명/`[오류]`에 vid=. 미매칭=vid=없음+등록명. 진단도구도 옵션별 `vid=…·판매상태·지표·재고` 한 줄. 검증 3종 통과·실출력 확인. **grep vid=<값>으로 한 상품 전과정 추적 가능.**

**⭐2026-09-20-3 (라이브 검증 준비·커밋 2e3e2a1): 다른 PC(사무실) exe 배포 준비 완료.** 소유자=exe 재빌드 배포·읽기전용 진단 먼저. **`쿠팡진단.exe`(console·읽기전용)** 를 앱과 같은 onedir 에 함께 빌드하도록 spec 수정(MERGE 공유의존성 중복제거·엔트리 tools/verify_login_discover_live.py). **빌드·실행 검증 완료**(두 exe 생성·`쿠팡진단.exe --list`가 구글시트 관리대장 26계정 로드). 진단 도구 버그수정(4튜플 언팩)·입력을 앱과 동일 로드(`_load_input`)·vid/판매상태/재고 콘솔 출력(마스터 미변경). `deploy/라이브검증_안내.txt`(백업→진단exe→앱). **다음=사무실서 build.bat 빌드→dist 복사→쿠팡진단.exe로 productStatus 원문값·vid 확인→마스터 백업 후 앱 ①판매수집.**

**⭐2026-09-20-2 (추가 지시 반영·커밋): ①vid 변경 마이그레이션 정책 확정** — vid=상품당 1개(2개 없음), **첫 적용 시 vid가 기존과 다르면 이전 데이터 삭제하고 새로 시작**(같은 vid만 승계). `_process_account`가 같은 vid=승계·다른 vid=`delete_product_block`으로 삭제(workbook `blocks_with_registered_name`+`delete_product_block` 신설). **②계정 삭제 시 관련 시트 제거=기존 구현으로 이미 처리**(delete_account 시트·메타 + gsheet delete_accounts 계정목록 행·통계 시트, 새 코드 불요·테스트 통과). 검증 simulate[12]·verify_offline[12]·전체 3종 통과.

**⭐2026-09-20 (구현·커밋): VID 출처 변경 1~6단계 완료·오프라인 검증 통과·⚠라이브 확인 남음.** gsheet-only 정체성 재설계를 실제 구현. **3단계**: vid 출처 반전 — 헤더 이름칸 'VID :' 꼬리(인메모리 `_block_vids`, `_reindex`가 파싱), `product_vids`=이름칸 읽기·`set_product_vids`=이름칸 즉시 렌더·숨김 `_상품ID` vid 3열 폐지(무손실 이관). **4단계**: `ensure_product_block(rank_rows=,registered=)`·`has_keyword_section`(2차 블록 판정)·`pad_keyword_rows` 가드. **5단계**: `_fill_product_metrics`=옵션(vid)별 지표(합산 제거)·③ 매칭=`sibling_vids`(같은 등록상품명 옵션 vid 합집합=아이템위너 놓침 방지)·**set_display_name(노출명 교체) 중단**(3곳). **6단계**: `_login_and_discover`가 `fetch_vendor_inventory`를 vid 출처로(폴백=판매분석)·`_process_account` 상품→옵션 블록 루프(대표=첫 옵션 키워드+순위·2차=지표만)·마이그레이션(옛 블록 vid로 찾아 대표 옵션명 정규화·과거 이력 승계). **소유자 결정 반영**: 블록명=등록상품명+옵션라벨(다중옵션만·단일=등록명)·대표옵션 과거이력 승계·②③ gsheet 의존 안 생김(vid가 마스터 이름칸에 있음). **7단계(판매상태 productStatus화) 완료**(소유자 추가 지시): `sale_status_of`/`sale_status_by_vid`로 productStatus(전 상품·판매자배송 포함)를 판매상태 출처로(상품조회 실패 시 RFM 폴백)·`apply_sale_status`가 문자열·bool 둘 다 수용 → **판매자배송 상품도 판매상태 경고 커버**(옛 RFM만 한계 해소). ⚠원문 enum 미확정=방어적 매핑·라이브 로그로 확인. 검증: verify_offline[8·12]·simulate[11 다중옵션][9 노출명중단]·verify_gsheet 3종 통과. **⚠라이브(사무실 로그인·실제 마스터)에서 vendor-inventory 실응답 필드·마이그레이션 실동작·순위 sibling 매칭 최종 확인 필요.** 상세 [[feature-vid-source-from-product-list]].

**⭐2026-09-20 (분석·문서화만·코드 미변경)**: **VID 출처 변경 설계·API 스펙 전량 확보.** 소유자가 사무실PC 원격(폰)으로 상품조회/수정(`vendor-inventory/search`) DevTools 캡처를 단계적으로 제공 → 엔드포인트·요청본문·응답필드·페이지네이션·인증 **전부 확정**. `POST /tenants/seller-web/v2/vendor-inventory/search`(application/json·세션쿠키+xsrf). 리스팅=`productName`(매칭키)·`vendorInventoryId`(그룹키)·`registrationType`(NORMAL/RFM)·`productStatus`(판매상태·전상품). 옵션=`vendorItemId`(vid)·`itemName`(라벨)·`registrationType`(둘다판별). **⚠재고는 이 소스 제외**(등록시 임의값·부정확)→재고=RFM API만. **⚠노출상품ID(productId) 응답에 없음**→그룹핑은 `vendorInventoryId`. 확정설계=옵션분리(다중옵션만)·대표=첫옵션만 순위·둘다=RFM만·vid=구글시트만 저장·방문자/판매=vi-detail 유지·대장 vid역기록 금지(재고역기록은 유지). **반영 완료**: DESIGN §0-00000·§2.3·§8-G, 메모 [[feature-vid-source-from-product-list]](API스펙 블록)·[[feature-sale-status-mismatch-flag]](productStatus 개선여지). **⚠미구현·승인 후 착수**([[analysis-request-no-code-change]]). 라이브 검증은 사무실 세션 필요(집=2차인증).

**⭐2026-09-18 완료(커밋 b8bdc20)**: **대장↔쿠팡 정밀 매칭**(오매칭 차단) — 사용자: 계정에 위탁/직접 판매 상품이 섞여있고 쿠팡 상품조회론 구분 안 됨(대장이 유일 기준·대장명≠쿠팡명 가능). 느슨한 옛 매칭이 비관리 상품에 오매칭돼 통계 섞임. `product_match._assign` 정밀 우선으로 교체(괄호정확→핵심어 IDF게이트+재현율0.55+마진0.15, 미달=미매칭 공란·규격토큰 제외·augment 동일). 소유자 결정=**정밀 우선만**(대장 역기록[쿠팡정확제목/vid 확인루프]은 보류=원하면 나중에). 검증 verify_offline[11]·simulate·gsheet 통과. **09-17 야간 무인 전체실행 완주 확인**(①25계정 로그인성공·차단0·반달컴퍼니mrc098 비번오류 스킵 / ②키워드 / ③순위 실순위66·50위밖320 / 판매상태경고 라이브작동·isSaleSuspended 필드 실재확인 / 그로스재고30 / 구글시트 통계23+계정목록 반영). ⚠조치: 반달컴퍼니 비번수정·벅스 상품명'--'·하성진 토탈사이언스 키워드.

**⭐2026-09-17 완료-4(커밋 2f532e2)**: **관리대장 계정 추가/삭제 반영** — 사용자: "관리대장은 사업자 추가/삭제됨, 계정 삭제 시 해당 줄 삭제". 결정=**줄이 완전히 사라진 계정만 완전 삭제**(계정목록 행+통계 이력 시트), **판매중지로 남은 건 유지+경고**(판매중지 경고 기능과 양립), 로그인 실패는 삭제 안 함. 구현=`InputList.ledger_account_ids`(줄 존재 계정ID·판매중지 포함)·`workbook.delete_account`(시트+메타)·`gsheet_index.delete_accounts`(계정목록 행+통계시트, 헤더기반 계정ID 탐지)·`pipeline.run_full` 판정→삭제·`_push_gsheet(removed_accounts=)`. 검증 verify_offline[10]·verify_gsheet_offline[3c]·simulate 통과. **1회 정리 실행 완료**: ①대표자 채움(관리대장 25계정, 마스터 100행)·②고아 6계정 삭제(비앤유·디케이유니버스·세제곱·콰이어트랩·스마트플라자·글로벌셀러창업연구소 — 마스터 22시트 남음·구글시트 계정목록 16행+통계 6시트 삭제). 마스터 백업 2개(대표자추가전·계정삭제전). ⚠커밋 대기.

**⭐2026-09-17 완료-3(커밋 c33da7e)**: 사용자 요청 3건 — ①**계정목록 대표자 컬럼** 추가(구글시트+엑셀, A=대표자·8열, `_계정정보` 4열, 옛 7열 구글시트 `_ensure_rep_column` 자동 마이그레이션·read_marketing 헤더기반). ②**키워드 띄어쓰기=유의미**(09-15 병합 되돌림: '캠핑타프'≠'캠핑 타프' 노출순위 다름 → `_assemble_candidates` by_norm 제거·exclude/add_product_keywords strip정확일치, 검색타이핑은 원래 공백유지). ③키워드 처리 확인=채워진 것만 순위검색·공란보존·직원 4개초과 추가분도 검색(기존동작+②로 변형도 검색됨). 검증=verify_offline[9]·verify_gsheet_offline(마이그레이션 포함)·simulate 통과. ⚠**주의**: `_계정정보` 3열=판매수집일과 충돌해 대표자를 4열로(초기 실수 교정). 새 대표자 값은 다음 파이프라인 실행이 채움(기존 마스터는 apply_style만으론 공란·정상).

**⭐2026-09-17 완료-2(커밋 a11d330)**: **관리대장 그로스재고 공란 버그 2건 수정** — 사용자가 대장 AD열(그로스 재고 자동갱신) 일부 상품 공란 발견. 원인: ①구분이 개인→로켓그로스/둘다로 바뀐 블록에 `ensure_product_block`이 라벨만 바꾸고 **재고현황 행을 안 만듦**(재고 저장 자리 없음)→저장 자리 없어 `product_inventory`=None→매칭후보 아님→역기록 공란. `workbook._add_metric_row`로 재고행 자동추가(개인→로켓그로스/둘다 시, 멱등). ②`pipeline._fill_product_metrics`가 재고를 `KIND_CONTRACT`만 기록·**`KIND_BOTH`(둘다) 누락**→`KINDS_WITH_INVENTORY`로 확대. 다음 ①판매수집이 자동치유. 실측 마스터 방치 13개(웰빙곳간10·DW·명진·이종훈). 로그: 09.16 웰빙곳간 재고 52개 조회됐으나 발견6/대장17이라 11개 vid보강+개인구조라 미기록. verify_offline[7] 회귀테스트 추가. 상세 [[seldoc-output-format]].

**⭐2026-09-17 완료(커밋 a11d330·두 작업 함께)**: **판매상태 불일치 경고** 구현 — 대장=판매중지인데 쿠팡=판매중/부분판매중이면 결과파일 최신 날짜칸에 "판매중" 진한적색(C00000). collector(`_parse_inventory_status`·fetch_inventory 3튜플)→pipeline(`_login_and_discover` 4튜플·`_finish`가 `apply_sale_status` 1회)→workbook(`_상품ID` 7열·set/sale_status·sale_active·apply_sale_status·apply_style 렌더)→gsheet 미러링 자동. **오프라인 실증 통과·기존 회귀 없음.** ⚠**라이브 확인 남음**(①판매수집 로그인=사무실서 isSaleSuspended 필드 실재 확인·개인상품은 미상). 상세 [[feature-sale-status-mismatch-flag]]. ⚠아직 git 커밋 안 함(사용자 지시 대기).

**⭐이전 대기작업(2026-09-16)**: 09-15 저녁 ③순위 59% 중단분 수동 마무리(앱 "③순위 반자동")·반달컴퍼니(mrc098) 비번 정정.

**⭐2026-09-16 (최신-3)**: ①날짜 라벨을 **년도 없는 '월.일'**로 통일(당분간·사용자요청) — `%m.%d` 전구간·`_parse_date` `%m.%d` 추가·기존 마스터도 재포맷 완료(값 2194셀 유실0). ②**콘솔창 깜빡임 해결**: 무인 `--auto`는 pythonw(콘솔없음)라 browser.py 보조명령(파워셸·taskkill·Chrome)이 명령마다 검은창 깜빡 → subprocess 4곳에 `CREATE_NO_WINDOW` 추가(직접실행은 원래 안 뜸=방식 동일·실행방법 차이). ③용어 '배선' 싫어함([[plain-language-no-jargon]]). **커밋 완료 cff976f**(날짜규칙+월.일+콘솔숨김·최신-2와 함께). **⭐라이브 무인 완주(2026-09-16 19:31→09-17 04:59)**: 스케줄러 `쿠팡애널리틱스_야간무인` --auto 전 과정 완주. ①판매 **25/25**·②키워드 완료·③순위 **385키워드·실순위(1~49) 79건·차단 0**·그로스재고 16개·구글시트 반영 완료·04:59 자연종료. **결과 파일 검증**: 09.16 컬럼 생성(판매230·순위384칸)·**날짜=실행날짜 09.16·년도없는 월.일**·시계열 09.09~09.16 연속. **콘솔창 안 뜸(사용자 확인)**·앱창은 2번모니터에 정상 표시. **이번 세션 변경 전부 라이브 검증 완료.** ⚠앱 창이 없어 보이면 2번(오른쪽) 모니터 확인.

**⭐2026-09-16 (최신-2) 날짜 컬럼 규칙 변경·소급 정리 완료**: 결과파일 날짜 컬럼을 **작업 실행날짜 기준**으로 확정([[date-column-run-date-rule]]). ①라벨=실행날짜(순위=실행일·판매=전일 D-1 같은 컬럼) ②새벽 넘겨도 시작일 기준(date_label 진행중파일 저장·재개 복원) ③**미실행/중단 날짜=날짜만+공란**(시트별 첫날~마지막일 연속·`normalize_date_columns`+`ensure_date` 갭필·`apply_style` 멱등) ④과거 D-1 라벨은 그대로(앞으로만·경계 빈하루 정상). 코드=workbook.py/pipeline.py(`date_label`)/app_qt.py·app.py(`_run_dates` 3튜플)/tools/normalize_dates.py. **실제 마스터 소급 적용 완료**(09.11 등 25칸 삽입·값 2194셀 유실0·백업 `…날짜정렬전_260916_181634.xlsx`). 설계서·CLAUDE.md 갱신됨. ⚠**아직 git 커밋 안 함**(사용자 지시 대기). **18:00 --auto 관찰**: 25계정 세션 전부 만료→계정마다 창 띄워 확인·닫기 반복(=콘솔 깜빡임, 크래시 아님), [8/25]서 프로세스 종료(크래시로그 없음)·좀비크롬 없음·판매수집 0(집/비사무실이라 로그인 불가 정상). --auto는 "이어쓰기"로 어제 무시·오늘(09.15 D-1→라벨은 이제 실행일) 새로 시작 확인.

**⭐2026-09-16 업데이트(최신)**: ①**상세페이지 이미지 추출** 기능 신설(A안 CDP attach·[[detail-image-extraction]])·②**재부팅 자동복구** 구현([[reboot-recovery]])·③app_qt **창 크기** 화면제한+자유리사이즈 수정·④스케줄러 스크립트 인코딩(BOM) 수정+python소스용 `install_schedule_py.bat` 추가. **이 세션 작업 커밋 완료.** ⚠**미결**: (a)09-15 저녁 전체실행이 **01:29 Windows업데이트 재부팅으로 중단**(③순위 59%=249/424키워드, 판매25/25·키워드 완료). 마스터 `통계.xlsx` 01:29저장분 안전. 마커 없어 자동복구 안 됨 → 앱 **"③순위(반자동)"로 수동 마무리** 필요(순위41%+그로스재고+구글시트). (b)사용자가 `install_schedule_py.bat`(관리자) 실행해 스케줄러 등록 + Windows "재시작후 자동로그인 완료"는 켜둠. (c)사용자 선호=**쉬운말·전문용어 금지**([[plain-language-no-jargon]]).

**⭐전체실행 재설계(2026-09-15, 확정·운용): offscreen 전무.** 전체실행 = **①반자동 판매 → ②키워드선정(노출측정 없음·단 쿠팡 자동완성=연관검색어는 사용) → ③반자동 순위**. 구현=`do_run_full`(app_qt·app.py)이 `run_full(keywords_off=True, sales_semi=True, skip_ranks=True)` → `select_keywords_stage()` → `track_ranks_stage(semi=True)` 순차. **무인 `--auto`도 동일 반자동 조합**(start_auto, 무인이어도 방식=반자동). Task Scheduler(`deploy/install_schedule.ps1`)가 18:00 `--auto` 시작·06:00 자동종료. `_backfill_ranks` 가드=`not skip_ranks and not keywords_off`(offscreen 순위경로 사문화). 근거=**offscreen 자동은 warm IP서 0/237 전멸 vs visible 자동타이핑 237/237 완주** 라이브 실증([[semi-auto-rank-and-exposed-name]] [[rank-antiblock-circuit-breaker]]). SSOT=DESIGN §0-0.

**⭐이번 세션(2026-09-15) 완료·커밋**:
- **그로스 재고 역기록** — 전체실행·무인 종료 시 수집 재고를 관리대장 `AD` 컬럼에 씀. 매칭키=계정+등록상품명 **유사도(IDF+핵심토큰 게이팅)**. 라이브 75행·19매칭·오매칭0, 이전 오기록(베타글루칸=2→0) 교정. 상세 [[input-ledger-format]]. (커밋 7df0ecb·af2096f)
- **띄어쓰기 변형 합치기** — 쿠팡 동일취급 → 선정·워크북적재·시트미러링 전 구간에서 공백/대소문자 변형 dedup(네이버 검색량 큰 표기 유지·정체성 보존). 실측 마스터·라이브시트 408키워드 전부 무공백·중복0(기존 동결도 이미 clean=손댈 것 없음). 상세 [[keyword-methodology-ai-anchor]]. (커밋 d77e206·b9e0bb4)
- **로그인 비번 1회 오류=재시도 금지**(커밋 bf898a3) — `classify_login()='error'`(비번오류·계정잠금·휴면)이면 반자동 재시도 건너뜀(재제출 안 함)+`LoginCredentialError`로 그 계정 `done` 처리→야간재개·같은날 자동재개가 다시 제출 안 함(위탁계정 5회=잠금 방지). 소프트차단(폼정체·Akamai)만 반자동 1회 재시도 유지. 상세 [[login-block-session-first-circuit-breaker]]. **⚠ 비번 정정 후 재시도 시점**: 새 날=자동, 같은날은 비번오류 계정만 실패했으면 진행분 삭제→자동, **다른 계정도 미완료로 진행분 유지 시 "이어서 하기"는 done으로 스킵**→"오늘 처음(다시) 하기"/"전체 새로 시작"으로 강제 재시도. (로그인은 전체실행 ①판매수집 2차 패스에서만·세션 살아있으면 로그인 없이 수집.)
- 메모리 압축(gsheet-unified 75→21·keyword-methodology 45→22·input-ledger 32→21줄)·설계서 정밀대조 정정(KW_TRACK_N 8→4·KW_GEN_N 40→20·Score 100→95·§9 모듈목록 현행화 등)·CLAUDE.md 정리(커밋 e048b98).

**최근 라이브(2026-09-15 새벽)**: ①판매(반자동) 23/24계정 수집(반달컴퍼니 mrc098 **비번오류** 스킵—사용자 확인 필요). ③순위(반자동) 237/237 완주·차단0·실순위(1~49위) 32건. **특이: 정해성(김현창) 빔프로젝터거치대(vid 95878234043) 09-10 1위→09-12 50위밖 급락**(품절·바이박스 점검 권장).

**config.py 순위/키워드 세부값**(설정탭 UI 없음 [[runtime-ui-and-always-on]]): RANK_NAV_DELAY 45/75 · RANK_COOLDOWN 900·MAX2(자동) · RANK_SEMI_COOLDOWN 1800·MAX4·AUTO_MAX_MISS3·AUTO_WAIT40(반자동) · RANK_SCAN_MAX 50(자동)/SEMI300 · KW_TRACK_N 4·**KW_MAX_TRACK 10**(2026-09-20 7→10·가변)·ADD_PER_DAY2 · SALES_VID_WINDOW_DAYS30 · LOGIN_UNATTENDED True.

**⏭ 다음(권장)**: ①**반달컴퍼니(mrc098) 비번 관리대장에서 정정** → 재시도는 "오늘 처음(다시) 하기"/새 날 전체실행에서(같은날 "이어서 하기"는 done 스킵 주의). ②순위/판매 라이브는 **핫스팟(깨끗한 IP)**·**사무실만**(2차인증 위치기반·위탁계정). ③45~75 무차단 지속 시 간격 30~50 단계적 하향. (2026-09-14 세부 변경 7건[서식개편·간격하향·마우스입력·지문정합·실행3택·①반자동]은 완료·git 이력.)

관련: [[rank-antiblock-circuit-breaker]] [[semi-auto-rank-and-exposed-name]] [[input-ledger-format]] [[keyword-methodology-ai-anchor]] [[gsheet-unified-spec]] [[pipeline-3stage-separation]]
