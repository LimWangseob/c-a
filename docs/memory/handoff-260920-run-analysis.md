---
name: handoff-260920-run-analysis
description: ⭐인수인계(2026-09-20 밤) — 18:00 무인 실행 분석 + 발견된 심각 문제 2개(fix③ 복원 오염·복원+옵션분리 중복잔재). 새 세션이 정밀 재분석·수정. 파일=output/_인수인계_260920/
metadata: 
  node_type: memory
  type: project
  originSessionId: 82e67a4e-addd-4c02-bcbb-8e2fed28f4b4
  modified: 2026-09-20T16:30:04.418Z
---

**✅후속 세션 구현 완료(2026-09-20 밤·미커밋 대기):** 근원(마스터가 다른 폴더에서 없어 복원됨)을 **폴더 재설계로 제거** + 버그2개 수정 + 차단방지. ①**경로=개발·운용 동일·평평(2026-09-21 소유자 최종)**: 하위폴더 안 나눔. `apppaths.data_root()`=base_dir(앱 폴더·env `COUPANG_DATA_ROOT` override), 한 폴더에 코드+output+data+config.json, 상대경로, **폴더명도 동일(`coupang-analytics`)**. → 재배포해도 같은 폴더라 마스터·크롬프로필 보존(=**cold 프로필 차단원인 해소**). ②**보존 vs 삭제**: output·data·config=삭제 금지, 업데이트=새 zip 의 coupang-analytics\ 를 같은 자리 덮어쓰기(그 3개는 zip에 없어 보존). build.bat `_씨앗\`에 노트북 통계 마스터만 동봉→install.ps1 첫 설치 때만 output로 복사(프로필=947MB·교차이전 제한이라 미동봉·운용PC는 자기 data 휴지통 복원 사용). ③**설정=config.json**(`appconfig`, 비밀 아님만·키는 credstore, 시작 시 레지스트리 동기화). ④버그1=복원 시 `계정목록`(공백없음) 탭 제거 + `account_sheets` 공백무시 제외. ⑤버그2=옵션분리 잔재정리에 **vid 없는 옛 블록(복원분) 삭제** 추가(new_names·미매칭 보존 가드). 검증=verify_offline·simulate(신규 시나리오13 복원잔재·14 계정목록제외)·verify_gsheet 전부 통과. **핫스팟 권고는 폐기**(소유자 지시=18시 이후 업무용 PC만·과거 핫스팟도 실패=IP보다 프로필/행동이 원인). ⚠라이브 배포·야간 실행 확인 남음. 상세 [[exe-packaging-deploy]]. 아래는 원 분석(참고).

---
**새 세션 인계(2026-09-20 밤).** 긴 세션이라 새 창으로 넘김([[recommend-new-session-when-degraded]]). 오늘 18:00 무인 실행의 실파일/로그를 **`output/_인수인계_260920/`** 에 복사해 뒀다(git 미추적). 새 세션은 이 파일들로 **정밀 재분석 + 아래 2개 버그 수정**을 이어간다.

## 인수인계 파일 (output/_인수인계_260920/)
- `run_log_260920_180002.log` — **주 로그**(2240줄, 18:00→21:21, ①②③ 전 과정)
- `run_log_260920_172905.log` — 17:29 짧은 로그(입력·비번만·무해)
- `쿠팡데이타분석_통계 (2).xlsx` — **오염된 마스터**(중지 시점)
- `쿠팡데이타분석_통계 (1).xlsx` — 구글시트 결과 다운로드(같은 오염)
- `쿠팡데이타분석_진행중 (1).xlsx`·`.json` — ③ 이어서용(핫스팟 재개)
- `쿠팡데이타분석_통계_260920.xlsx` — 오늘 스냅샷 / `_실행단계.json`(stage=ranks)

## 오늘 세션 커밋(로컬 master·**미푸시**·원격 없음)
ac12649(옵션분리 후속5건: 계정목록 상품별·검색량채움·**fix③ restore_master_from_gsheet**·서식그룹화·용어) → 69e7561(중단방식·되돌림) → 53174ef(**키워드=구글시트 셀값 기준**·하드삭제·AI톱업제거·상한10) → 7f6511d(**작업전 자동백업 backup_sources**·정밀시뮬 stale수정) → 5867350(**무설정 배포** export/import 설정이식) → a35cf6f(zip 폴더째) → 809fb90(**자동실행 삭제 파일** 자동실행_삭제.bat/remove_autorun.ps1) → 71d6247(재고 진단로그 덤프 제거). 빌드=`dist/쿠팡애널리틱스_배포.zip`. **이 PC 자동실행 스케줄러 2개 삭제 완료**(관리자 승격).

## 18:00 무인 실행 결과 요약
- **① 판매수집 25/26**: 로그인 실패 3(커스텀존 unipang·반달컴퍼니 mrc098=비번오류, 하성진 lslfgh=Akamai 5분초과×2). 미매칭 7계정(대장명≠쿠팡명).
- **② 키워드**: 동결(시트값 그대로)53·AI신규21·실패2(대장 placeholder '벅스 --'·'하성진 토탈사이언스'). **키워드=시트값 로직 정상 동작**(휴라엘 담당자편집 목디스크/캠핑타프 반영 확인).
- **③ 순위: 13/441만 채움(전부 '50위밖'), 428 공란**. **20:00 사무실 IP Akamai 차단**(쿠팡 "요청하신 페이지 사용권한 없음" 페이지 캡처로 확인). ⚠소유자 의심="URL못찾아 차단?"→**로그 시간순으로 반증**: 차단 전 13건 검색창 정상, "검색창 못찾음"은 전부 차단 후(=차단페이지엔 검색창 없음). **진짜 IP차단·해법=핫스팟(깨끗한 IP)에서 ③ 이어서.**
- **구글시트**: 통계 25시트 미러링 OK, **계정목록 sync 3회 실패**(Sheets 400 Invalid requests).

## ✅정밀 재분석 완료(2026-09-20 밤·새 세션) — 근원·버그2개·수정방향 확정, **수정 보류(소유자=분석만)**
- **근원 확정**: 18:00 무인이 **마스터 없는 폴더에서 실행**돼 복원 작동. 근원의 근원=`_master_path`가 상대경로 `output/`(pipeline.py:51)+배포exe가 CWD를 exe폴더로 고정(apppaths.py:21) → **새 무설정 배포본을 새 폴더에 풀어 실행→그 폴더 output/ 비어 마스터 없음**. **이 저장소(D:\coupang-analytics\output) 마스터는 9/18 정상본(128KB·27시트·`계정 목록` 하나·웰빙곳간 22블록) 손대지 않고 생존** → 18:00은 여기가 아니라 배포 폴더에서 돎(정상본이 안 덮임이 증거). **복구=이 9/18 정상본 기준 재수집·오염본 폐기**(배포폴더서 돌리려면 그 output/에 정상본 먼저 복사).
- **버그1 수정방향 확정**: `restore_master_from_gsheet`가 `download_xlsx`로 결과시트 통째 내려받아 인덱스탭 `계정목록`(공백없음·gsheet_index.py:29)까지 마스터로 들어옴. 마스터 필터 `_SPECIAL_SHEETS`엔 `계정 목록`(공백·workbook.py:50)만 → `account_sheets()`(workbook.py:219)가 `계정목록`을 통계로 오인→미러링(로그 1603)+sync_index 충돌→400 Invalid requests[62](로그 1629). **수정=①restore 직후 `계정목록`(공백없음) 탭 삭제(원천) ②account_sheets가 공백무시로 인덱스명 시트 항상 제외(방어)**.
- **버그2 수정방향 확정**: 구글시트 미러엔 숨김 `_상품ID` 없고 옛 블록 이름칸에 `VID :` 꼬리도 없어 **복원블록 vid=빈값**. 옵션분리 잔재정리(pipeline.py:766-771)가 `if vidset and stored and not(vidset&stored)`라 **stored(옛vid) 있어야만 삭제** → vid없는 복원블록은 못지움. `blocks_with_registered_name`(workbook.py:1308 p==reg)은 **찾긴 함**, 삭제만 안 됨. 잔재=**다중옵션 상품만**(옛블록명=등록명, 새=등록명+옵션라벨→옛것 고아). 실측: 복원 웰빙곳간 97블록(정상22)·우슬등/사과초모식초/맥문동환30포가 [개인상품·VID없음]+[옵션블록·VID] 공존. **수정=조건 확대 "(옛vid있고 새vid와 완전다름) OR (옛블록 vid없는데 이번에 vid있는 옵션블록 생성)". 안전장치: 새 블록명은 삭제제외·vidset 있을 때만(미매칭=대장명추적 vid공란 상품은 보존)**.
- 검증계획=verify_offline/simulate/verify_gsheet + 실파일 재현. (아래 원본 진단은 참고로 유지)

## ⭐발견된 심각 문제 2개 (수정 보류 — 새 세션이 정밀분석+수정)
**근원 = 18:00에 마스터가 없어 fix③(restore_master_from_gsheet)이 돌았다. 원래 마스터가 있었으면 깔끔했을 것.**
1. **fix③ 복원이 '계정목록' 탭 오염**: 구글시트 index 탭명=`계정목록`(공백없음, gsheet_index.INDEX_SHEET_NAME)인데, 마스터 특수시트 필터 `_SPECIAL_SHEETS`엔 `계정 목록`(**공백 있음**, workbook._INDEX_SHEET)만 등록. → 복원본의 '계정목록'(공백없음)이 필터 안 걸려 `account_sheets()` 오염 → push_statistics가 '계정목록'을 통계시트로 미러링(로그 "통계 시트 '계정목록' 반영 128행") + sync_index 충돌 → **Sheets 400**. **증거**: 마스터에 '계정 목록'+'계정목록' 둘 다(29탭). **수정안**: restore 직후 '계정목록'(공백없음) 탭 제거/정규화, 또는 INDEX_SHEET_NAME을 account_sheets 제외에 추가.
2. **복원+옵션분리 중복 잔재**: 마스터 없어 복원(옛 이름·옵션 없는 구조) 위에 **옵션분리 ①(새 이름·옵션별 vid 블록)** 얹힘 → 이름/vid 불일치로 **옛 블록=판매중지 잔재, 새 옵션블록=대량 신규**. **웰빙곳간 16상품 → 97블록**(옵션별 ~87 = 수량옵션 1~6개마다 블록[설계상 정상이나 방대] + 판매중지 잔재 ~10 = 알부민'맥스'/'MAX', 우슬등, 사과초모식초 등 옛/새 중복). 계정목록 128행·판매중지 54(잔재 다수). **수정안**: 복원+옵션분리 마이그레이션이 옛 이름 블록을 정리(delete_product_block)·'계정목록' 오염 제거.
- **이 오염 마스터는 그대로 쓰기 곤란**(데이터값은 맞지만 목록/블록 중복). 09-18 정상 마스터(`_통계_260917.xlsx` 등)로 재수집 판단 권장.

## 새 세션 할 일(정밀 재분석 후)
1. **마스터가 왜 사라졌나 규명**(근원): 새 무설정 배포본 설치로 **작업폴더(set_workdir)가 바뀌어** 마스터가 다른 위치에 생긴 건 아닌지 확인.
2. **버그 2개 수정**(위): fix③ '계정목록' 오염 + 복원/옵션분리 중복 잔재. 검증=verify_offline/simulate/verify_gsheet + 실파일 재현.
3. **③ 순위**: 핫스팟에서 이어서(진행중 (1).xlsx 상태로 재개).
4. 운영 정정: 비번(unipang·mrc098), 대장 placeholder(벅스 '--'·하성진 토탈사이언스), 미매칭 7계정 상품명.
- 관련: [[session-current-state]] [[feature-vid-source-from-product-list]] [[exe-packaging-deploy]] [[daily-stats-keyword-freeze]] [[fix-from-real-evidence]].
