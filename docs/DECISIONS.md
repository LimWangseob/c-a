# DECISIONS — 확정된 정책·설계·구조 결정 (SSOT)

형식: `YYYY-MM-DD · 요지 · 사유`

2026-09-24 · 재고칸 = 재고현황 있으면 값(0=품절)·없으면 판매중지 무관 "미입고"(옛 판매중지→공란 폐기·`_block_sellable` 제거) · 제한계정 all-SUSPENDED여도 재고칸이 텅 비지 않게(웰빙곳간 47블록중 31공란 실측).
2026-09-24 · 판매상태(productStatus)는 쿠팡 그대로 존중, 상품블록 '판매상태' 지표행(`M_SALE_STATUS`)에 실행일마다 기록(재고칸 아님·보정 안 함) · 담당자 정정용 상시표기(소유자 선택=지표행 방식).
2026-09-24 · 업번들 잔재 블록은 매 수집 시 vid 기준 자동삭제(`_purge_upbundle_blocks`) · 리스팅 전체 업번들로 추적 제외된 상품의 고아 블록 정리(별도 도구는 담당자 변동과 충돌).
2026-09-24 · 3개 데이터 API 응답 **원문(가공 없음)** 을 매 실행 gzip 사이드카(`output/_raw/`)로 상시 보관(`SAVE_RAW_RESPONSES`) · 재수집·재빌드 없이 다양한 오프라인 분석·실측 감사추적(사용자 요청).
2026-09-24 · 중복 대표 판정 실측 결론(_raw 6계정 120그룹): 유령=registrationType NORMAL·실제=RFM 100% · 상품조회에 productId 없음 확정.
2026-09-24 · 죽은 중복 블록 정리는 **안전 sweep**(`_sweep_dead_duplicates`): 같은 상품군에 live 형제 존재 AND 블록 vid 전부 상품조회에서 소멸, **두 조건 모두**일 때만 삭제 · blanket("vid 없으면 삭제")은 판매중지 데이터 유실이라 금지(신규·취소선 미운영·대장삭제·변형·판매자배송twin·상품조회실패 보존) · 그룹키=마스터 블록명 접두(이름 드리프트 무관·live=vid 앵커).
2026-09-24 · 상품 헤더 셀(제목·VID 아래)에 로켓그로스 정보 묶음 표시 — **로켓그로스 판매일**(saleStartedAt 근사·"입고일"에서 명칭변경)·**최근입고 요약**(관리대장, 유형A). vid 파서 'VID :' 줄만 안전 파싱. **판매가는 헤더가 아니라 '판매가' 지표행(재고현황 아래)에 일자별**(마케팅 일환 변동 추적). 판매자배송은 로켓그로스 판매일 줄 생략.
2026-09-24 · **체험단 시작일부터 1개월**간 노출순위(키워드) 행 date칸에 배경색(`_promo_cols`·마케팅 종료일 무관) — 체험단 효과 구간 시각화.
2026-09-24 · **판매자배송(NORMAL) twin 블록은 삭제하지 않고 보존**(소유자 확정) · 로켓그로스+판매자배송 동시 등록 상품의 NORMAL twin 은 코팡에 vid 가 살아있어 죽은 vid 아님 → 판매중지 표기로 유지(데이터 유실 방지) · 완전 제거는 registrationType 배선 필요한 별도 후속(보류).
2026-09-24 · 계정목록 상태 열 = **관리대장 상태만**(소유자 확정) — 대장 판매중지 > 미수집 > 체험단 상태. 쿠팡 상품조회 판매상태(임시저장·승인반려 등)는 날짜별 '판매상태' 지표행에 이미 있어 계정목록에선 뺌(2026-09-22 productStatus 폴백 되돌림·상태는 소스별 위치 분리).
2026-09-24 · 관리대장 로켓그로스 입고 컬럼(요청일자·요청수량·작업수량·박스·파레트·완료일자·출고일자)을 결과파일 헤더 '최근입고' 한 줄로 요약(`_inbound_summary`·유형A) — 담당자 참고용, 역기록 미접촉.
2026-09-24 · 계정목록에 자동열 **'체험단효과'(I열)** 신설(소유자 확정) — 체험단 시작일 **직전 마지막 측정치→가장 최신 측정치** 점 비교. 판매=판매량 지표행 %변화·순위=모든 키워드 **최고순위(숫자 최소)** before→after(예 "판매 +38% · 순위 32→18 ↑"). 개선(판매↑·순위↑=숫자↓)=연초록·악화=연적색·데이터부족=공란. **직원 마케팅 E~G 뒤(끝)에 추가**해 미접촉(gsheet N_COLS 8→9·엑셀 A~I). 순위 '위밖'(미발견)은 정확순위 아님 → 제외.
2026-09-24 · 계정목록 열 확장 가드 추가(체험단효과 I열 400 수정·라이브 실측) — 옛 계정목록 그리드 폭이 `N_COLS`(9)보다 좁으면 9번째 열(체험단효과) updateCells가 그리드를 넘어 HTTP 400('Invalid requests[0].updateCells'). `GSheetClient.grid_col_count`(메타 columnCount)+`_grid_grow_requests`가 기록 전 부족분만큼 COLUMNS appendDimension으로 확장(행 확장과 동일 패턴). 검증=verify_gsheet_offline[3d].
2026-09-24 · 상품 블록 헤더 레이아웃 v4 확정(⏳미구현·새 세션 구현) — 좌측 A:B=라벨 칸(상품명 2줄·VID·판매방식·로켓그로스 3줄)·C:F=값·우측 지표 7줄과 정렬. VID 별도 줄(소유자 A안)→vid 저장 헤더셀→메타시트 이전(2026-09-20 되돌림·핀 먼저). 로켓그로스 3줄=쿠팡판매일 / 그로스요청일자+출고일 / 요청수량·작업수량·박스·파레트. 상품군 색=좌측 라벨 칸(살구↔민트)·라벨칸 폭=지표칸 폭·키워드 좌측확장. 계정목록 복귀 링크 문구='계정목록으로 이동'(✅반영). 계획=메모 handoff-block-layout-redesign.
2026-09-25 · 상품 블록 헤더 레이아웃 v4 **구현 완료**(오프라인 검증 통과·⚠라이브 남음) — 좌측 A:B=라벨 칸(상품명2·VID·판매방식·로켓그로스3 세로병합·상품군색)·C:F=값 칸(흰)·우측 지표 N줄과 정렬. 판매자배송(6줄)=로켓그로스 생략. vid 저장=메타 `_상품ID` col3(A안·옛 마스터 헤더꼬리 폴백 마이그레이션 `_migrate_vids_to_meta`)·판매방식=메타 col11(`product_kind`)·`_display_name`=상품명만. 키워드명 C→A열(병합 앵커)·A~E 좌측확장·사업자명 폐지. 열너비 A7·B7(=G14)·C:F 재배분. inbound_summary 2줄. 3단계 커밋(ed313fc·79d7290·+키워드)·핀 pin_apply_style S1·S2·verify_offline[7][12] 갱신. 사유=소유자 시안 v4 4회 반복 승인.
2026-09-25 · 레이아웃 v4 후속(소유자 피드백) — ① 상품명 값 칸(pos0 C:F)을 흰색→상품군 색(살구↔민트 교대)으로 칠해 상품명 강조·동일 상품군 시각 통합(나머지 값 칸=흰 유지). ② 같은 등록상품명(기본+옵션) 블록을 apply_style 마다 인접 정렬(_group_sibling_blocks/_regroup_sheet_blocks) — 옵션이 나중 실행에서 뒤늦게 발견돼 시트 끝에 붙어 분산되던 것(그룹 색/경계 안 묶임)을 형제끼리 붙여 치유. 행 insert 없이 값만 스냅샷→재기록(안전)·기존 분산 마스터도 저장 시 자동 치유. 기본↔옵션 구분=그룹 바깥 굵은선·내부 얇은선(기존). 핀 pin_apply_style S2·verify_offline(인접정렬 값무손실). 사유=소유자 최종 점검 피드백.
2026-09-25 · v4 후속(소유자 피드백) — 기본↔옵션(같은 상품군 내부) 구분선을 thin(연회색 BFBFBF·격자와 동일해 안 보임)→medium(진한 회색 808080)으로. 3단계 구분: 격자 thin < 기본↔옵션 medium < 그룹 바깥 thick. 구글시트도 SOLID_MEDIUM 반영. 핀 S5. // 참고(구글시트 포맷): 결과 통계 시트는 매 실행 worksheet_to_requests 로 전체 교체(병합해제→그리드 크기 재조정→전 셀 값+서식 덮어쓰기→병합 재적용)이므로 v4 새 포맷이 옛 포맷을 자동 대체(수동 재생성 불필요). 계정목록 인덱스 시트만 직원 마케팅 열 보존 위해 증분.
2026-09-25 · (치명 수정) 옛 마스터(v3)→v4 로드 시 키워드·순위 유실 방지 — 정밀 실측(옛 코드로 옛-포맷 생성→v4 로드)에서 발견: v3 키워드/소헤더=C열, v4=A열(병합 앵커). 그대로 저장 시 A:E 병합 비앵커 C값이 버려져 키워드·순위 전멸(판매지표·vid는 보존). _reindex(M_RANK) C 폴백 + apply_style _migrate_keyword_col(C→A 물리이전, 병합해제 후). verify_offline 옛→v4 무손실 시나리오 추가(기존 핀이 못 잡던 케이스). 교훈=마이그레이션 핀은 옛 포맷 합성으로 검증해야(신규 마스터만으론 회귀 못 잡음).
2026-09-25 · (사고+재발방지) 키워드 대량 유실 — 포렌식으로 09-25 01:23 실행이 원인 확정(직전 01:27 백업 C열 키워드 450 온전→실행 후 A열 24). 원인=키워드 C→A 이전(커밋 9cbdf71)은 있고 마이그레이션(b68b301)은 없던 빌드가 마스터 저장 시 A:E 병합 비앵커 C값 폐기→키워드명 소멸(순위·판매·vid 보존). 부차 원인=read_staff_keywords 가 C열을 읽어 v4 구글시트 키워드 미반영(6555e8d 수정). 재발방지: (1) _migrate_keyword_col 이 손상 소헤더('키워드'@A 소실) 자가복원(31f6577)·(2) verify_offline 에 v3→v4 무손실+소헤더 자가복원 시나리오·(3) tools/recover_keywords.py(백업→마스터 키워드 병합, --force-backup) 신설. 복구: output/백업/ 직전 온전본(통계_260924) 기준 447/450 복원(1 다중옵션 상품 3키워드는 구글시트 직원입력 보완). 교훈=포맷 이전은 마이그레이션+회귀테스트를 **같은 커밋에** 반드시 동반(뒤늦은 커밋 사이 빌드가 사고 유발). pytest 오탐(no tests=exit5)은 tests/test_regression_gate.py 로 해소.
2026-09-25 · 시트명 변경 계정 **자동 일원화**(소유자 시사점 "관리대장 정보는 담당자가 수시로 변경 → 항상 재작업") — 관리대장에서 담당자가 사업자명·대표자를 바꾸면 계정ID는 그대로인데 결과 시트만 옛 이름으로 남아 고아(전 상품 판매중지로 오분류·키워드 공란)가 되던 사고(예: 이종훈→원더폴리). `workbook.merge_account(src,dst)`+`_copy_product_block`: dst 없으면 rename(시트+메타 이관), dst 있으면 src의 dst에 없는 상품만 **이력 보존**(키워드·일자별 순위/검색량/판매지표·vid·판매방식·판매상태·판매중지·마케팅·입고)하며 이관 후 src 삭제. 파이프라인 `_consolidate_renamed_accounts`가 `_reconcile_ledger_accounts` **맨 앞**(삭제 판정 전)에서 대장 (계정ID→현재 사업자명) 기준 **전 계정** 스캔·병합 → 옛 이름이 판매중지로 오분류되기 전에 흡수. **매 실행 자동**(일회성 수술 아님)이라 전수 대응. 검증=verify_offline[16](rename·병합 분기·이력 보존·왕복). 사유=소유자 "1·2 진행·옛 5개 상품 이력 유지·다른 계정 전수 확인".
2026-09-25 · 일원화 시 **구글시트 계정목록·통계 시트도 연동 삭제**(소유자: 결과파일 계정목록에 이종훈·원더폴리 2행 잔존) — merge_account 는 워크북 시트만 정리하고, 구글시트 계정목록은 plan_sync 가 **행을 삭제하지 않아**(desired에 없으면 판매중지 표기·유지) 옛 이름 행이 잔존했다. `_consolidate_renamed_accounts` 가 일원화한 **옛 이름 [(옛사업자명, 계정ID)…]** 을 반환 → `_reconcile_ledger_accounts`→`_finalize_run`→`_push_gsheet(renamed_accounts=)` 로 전달 → `gsheet_index.delete_renamed_accounts` 가 옛 이름 계정목록 행+옛 통계 시트 제거. ⚠매칭키=**사업자명(B)+계정ID(D) 동시 일치**(delete_accounts 의 계정ID 단독 매칭은 병합 후 옛·새가 같은 계정ID를 공유해 살아남을 새 이름 행·직원 마케팅 E~G까지 지움) — 새 이름 행/시트는 push_statistics·sync_index 가 미러링·갱신. 검증=verify_gsheet_offline[3e](계정ID 공유해도 옛 이름 행만 삭제·원더폴리 보존). 게이트 6종+복잡도 초록.
2026-09-25 · (프로세스) 커밋마다 **4묶음 자동화**로 확정사항 무손실 인계(소유자 요청) — 세션 간 확정사항은 ①코드+설계서(git)·②결정기록 DECISIONS.md(git)·③메모리 3채널로 넘기는데, 메모리만 `%USERPROFILE%\.claude\...\memory`(repo 밖)이라 PC 교체·소실 시 유실 위험. 해결=pre-commit 훅(SSOT `tools/install_hooks.py`)에 `tools/sync_memory.py --stage` 추가 → **매 커밋 외부 메모리를 `docs/memory/` 로 미러링·스테이징**해 git 에 함께 저장(새 세션/새 PC 는 클론만으로 메모리 스냅샷 확보·실사용본은 여전히 .claude). 추가로 **코드 변경인데 DECISIONS.md 갱신 없으면 경고**(비차단·commit-with-design-and-memory 상기). 훅은 `.git/hooks` 라 같은 repo 면 세션 바뀌어도 유지, 새 클론은 `python tools/install_hooks.py` 1회 재설치.
2026-09-25 · (프로세스) 4묶음 자동화를 **push 에도 적용**(소유자 요청) — pre-push 훅에 `sync_memory.py --stage` 추가: 미커밋 메모리 변경이 있으면 자동 커밋(`--no-verify`)해 push 때에도 메모리 무손실. ⚠자동 커밋은 진행 중 push ref 에 미포함(git 특성) → "완료 후 다시 push" 안내(정상 흐름은 pre-commit 이 매 커밋 메모리를 이미 커밋하므로 이 분기 희소). pre-push = 메모리 백업 + run_checks(전체) + check_complexity. SSOT=tools/install_hooks.py.
2026-09-25 · (정리) 죽은 코드 물리 삭제(소유자 요청·vulture+전수 참조 근거) — ①모듈 `kw_shopping.py` 삭제(네이버쇼핑 API 2026-07-31 종료·`NaverShopCredentials`만 남았고 app.py 쇼핑키 UI 흐름이 수집만 하고 백엔드 미전달=죽은 기능) + app.py 네이버쇼핑 키 흐름 전체(import·self.naver_shop·shop_var·버튼·load_naver_shop·__naver_shop__ 로드/저장) 제거. ②참조 0건 메서드 3개 삭제: `gsheet_api.clear_values`·`workbook.inventory_by_registered_name`(inventory_by_biz 로 대체됨)·`workbook._roster`. 순 66줄 삭제. **보류(정책·동작)**: `_backfill_ranks`/`_measure_unfilled_once`=사문화지만 가드된 실제 호출부(pipeline)+simulate 모킹 존재 → 삭제 시 동작·테스트 변경이라 임의 삭제 안 함(별도 정책 결정 필요). 검증=게이트 6종+복잡도 초록·전 UI 컴파일 OK.
2026-09-25 · (계획·미구현) 소유자 9개 요구 정밀분석 확정 — ①②③앱시작 프리플라이트(2파일 백업+관리대장↔결과 싱크체크: 계정·사업자·취소선·상품명 유사도) ④계정목록 계정ID를 상품명 왼쪽+셀폭 자동맞춤 ⑤**사업자명 기준 그룹핑**(다계정ID 사업자=한 시트·계정ID를 상품 줄 속성·아키텍처 변경·오늘 계정ID기준 일원화와 충돌조정) ⑥**관리대장에 없으면 이력무관 완전삭제**(정책전환·백업이 과거 안전망·"판매중지 유지"·blanket금지 되돌림) ⑦계정목록 상태=대장 상태 싱크 재검증 ⑧비고=대장상태·날짜칸=쿠팡 상품조회 상태(대조) ⑨키워드 1건이라도 입력=담당자 간주(가변개수). 소유자 확정: 그룹키=**사업자명만**·진행순서=**⑤부터**. 각 항목 오류추적 로그. 구현은 새 세션(핀 먼저·감시파일 3개 리팩터). SSOT=메모 handoff-9items-spec-260925.
2026-09-25 · 항목5 **사업자명 기준 그룹핑** 구현(S1~S5, 소유자 2026-09-25 확정: 그룹키=사업자명만·계정ID=상품 줄 속성·계정마다 따로 로그인·수집) — 예전 "사업자명 1개=계정ID 1개=시트 1개" 가정을 깨, 같은 사업자명 다계정ID를 한 시트로 묶고 계정ID를 상품 속성으로 이전. **S1** workbook: `set_account_id`=계정ID 집합 누적(덮어쓰기 폐지)·`account_ids_of`·`set/product_account_id`(메타 col12)·판매수집 스탬프를 계정 단위(`_수집스탬프` 시트·키=계정ID)로 이전(다계정ID에서 둘째 계정이 이미완료로 스킵되던 버그 해결)·`account_due(account_id=)` 그 계정 상품만 판정·`_copy_product_block` 계정ID 이관. pipeline: 상품 블록마다 계정ID 태깅·스탬프/account_due 를 a.account_id 로 배선. **S2** input_list.`validate_input_list`: 같은 사업자명 다계정ID=치명 차단(시트 덮어씀 방지) → `[SYNC]` 병합 경고로 완화(소유자 결정). **S3** gsheet_index.`roster_from_workbook`: 계정ID=상품별(`product_account_id` 우선)·밴드를 계정ID→**사업자명 기준**(다계정ID=한 밴드). **S4** 엑셀 `_index_row` D열=상품별 계정ID(엑셀·구글 패리티). **S5** `_consolidate_renamed_accounts` 다계정ID 인지=시트의 대장-존재 계정ID들이 **모두 한 이름 동의 시만** merge, 발산하면 자동 병합 보류+`[SYNC]` 경고(수동 확인). 핀=verify_offline[17·18·19]·verify_gsheet_offline[6b]. 게이트 6종+복잡도 초록. ⚠라이브 확인·운용 PC 재배포 남음. 한계=발산(한 시트 두 계정이 다른 사업자명)은 자동 분리 안 함(경고만·수동). SSOT=메모 handoff-9items-spec-260925.
2026-09-25 · 항목①②③ **시작 프리플라이트**(소유자 2026-09-25) — 작업 시작 전 (a)2파일 백업[이미 `backup_sources`가 모든 실행 시작부에 호출·유지] + (b)**관리대장↔결과 비변경 싱크체크**(신규 `preflight_sync_check`). run_full 수집 직전 1회, 읽기 전용으로 `[SYNC]` 로그: ①대장O·결과X(신규/미수집) ②결과O·대장X(삭제 예정 ⑥) ③사업자명 변경(계정ID 동일·시트명≠대장 사업자명, 일원화 예정 ⑤) ④관리대장 취소선 제외. 불일치 판정 기준=**관리대장**(소유자). 실제 정리는 수집 후 ⑤⑥⑦이 수행(프리플라이트는 예고만). 핀=verify_offline[20]. 게이트 6종+복잡도 초록. ⚠라이브 확인 남음.
2026-09-25 · 항목⑥ **이력무관 완전삭제**(정책 전환, 소유자 2026-09-25 확정: 줄 사라진 상품도 완전삭제·가드 없이 항상 삭제) — 예전 "관리대장에서 줄이 사라진 **상품**=판매중지 표기(이력 보존)"를 **완전삭제**로 전환(계정 단위 삭제를 상품 단위로 확장). 근거=소유자 명시(백업이 과거 안전망·결과 깔끔). `input_list.Account.ledger_products`(줄 존재 전체=활성+판매중지/취소선) 신설(파서가 상품 행마다 기록)→`_login_and_discover`가 report_acc 에 전파→`workbook.reconcile_account(seen, ledger_products, delete_missing=True)`: seen(활성)=판매중지 해제·ledger_products에 있으나 비활성(판매중지/취소선=**줄 존재**)=판매중지 표기 유지·**둘 다 없음(줄 사라짐)=`delete_product_block` 완전삭제**. 반환=(newly, deleted). _process_account 가 delete_missing=True·[SYNC] 삭제 로그. ⚠**무결성 안전장치**: ledger_products 비면 완전삭제 스킵(대량 오삭제 방지) — 소유자가 거부한 '수집 실패 가드'와 별개(비가역 대량삭제 방지용). **_reconcile_ledger_accounts 의 "대장에 남았으나 비활성" 경로(1625)는 delete_missing=False 유지**(줄 존재=유지). **⚠_sweep_dead_duplicates(상품조회 API vanish)는 미접촉**(⑥은 관리대장 기준·sweep 은 API 기준, 축 다름·가드 유지). 핀=verify_offline[21](줄 사라짐 삭제·판매중지 유지·빈 ledger 안전장치). 게이트 6종+복잡도 초록. ⚠라이브 확인 남음.
2026-09-25 · 항목⑦⑧ **상태 표기 재검증**(소유자 2026-09-25) — ⑦계정목록 상태=**대장 상태만**: 현행 `status_of`가 이미 is_discontinued(reconcile=대장 기준)+체험단만 반영, `set_discontinued`는 reconcile(대장)에서만 설정 → 쿠팡 판매상태가 계정목록으로 새지 않음(코드 이미 정합·운용 PC 옛 코드 가능성). ⑧이중 비고=**대장 상태 vs 쿠팡 상태 분리**: 계정목록/비고=`status_of`(대장)·날짜별 판매상태=`M_SALE_STATUS` 지표행(쿠팡 productStatus, 실행일마다)로 이미 이원화. 소스 추적 로그=`apply_sale_status`의 대장↔쿠팡 불일치 경고(pipeline 1686)+reconcile 판매중지 로그+프리플라이트 [SYNC]로 커버. **코드 변경 없이 핀으로 불변식 고정**=verify_offline[22](계정목록=대장·판매상태 지표행=쿠팡·두 소스 독립). 게이트 초록. ⚠라이브 재배포 후 재확인(옛 코드 탓이면 재배포로 해소).
2026-09-25 · 항목⑨ **키워드 동결 규칙 재검증**(소유자 2026-09-25: 1건이라도 입력=담당자 간주·채워진 것만 검색량/순위·가변개수) — 현행 코드가 이미 충족: `_resolve_keywords`가 `product_keywords` 비지 않으면 AI 선정 생략(동결)·`_frozen_keywords`가 기존 키워드만 순위 측정·`_fill_frozen_search_volumes`가 공란 검색량만 네이버로 채움. 가변개수=`add_product_keywords`(빈 순위행 채움+초과분 insert)·`pad_keyword_rows`(4행 하한·공란 OK)·상한 `KW_MAX_TRACK=10`. 공란 순위행은 `product_keywords`가 미반환(=미검색). 직원 머지 `merge_staff_keywords`도 상한 내 가변. **코드 변경 없이 핀으로 불변식 고정**=verify_offline[23](1건=동결·AI 미호출·가변개수>4·공란 미검색). 게이트 6종+복잡도 초록.
2026-09-25 · 항목④ **계정목록 열순서 + 셀 폭 자동맞춤**(소유자 2026-09-25) — ④-a 열순서: 계정ID를 상품명 **왼쪽**으로(대표자·사업자·계정ID·상품명·체험단3·상태·체험단효과). gsheet `COL_ACCOUNT=2·COL_PRODUCT=3` 스왑·`_HEADS` 재정렬·`_auto_cells_request` C/D 값 스왑·안정키 메모=B(사업자)·직원 마케팅 E~G 불변. 마이그레이션 `_ensure_column_order`(moveDimension): 옛 순서(상품 C·계정ID D) 시트를 계정ID→C로 물리 이전(판매중지 행 포함 전 행·멱등, `_header_request`+증분 updates 는 활성 행만 재기록해 판매중지 행이 옛 순서로 남는 문제 해소). 엑셀 `_build_index`/`_index_row` C=계정ID·D=상품명(하이퍼링크). ④-b 셀 폭 **내용 길이 기반 자동맞춤**(엑셀·구글 패리티): 자동열은 헤더+데이터 최장 길이(한글=2폭)로 열별 min~max 클램프, 마케팅 E~G 고정. gsheet `_col_width_requests`(전체+증분 매 동기화)·엑셀 `_build_index` 스캔 기반 `column_dimensions`. 핀=verify_offline[9](엑셀 폭·순서)·[17]·verify_gsheet[3b](옛순서→moveDimension)·[6c](폭 클램프). 게이트 6종+복잡도 초록. ⚠라이브 확인·운용 PC 재배포 남음.
