# DECISIONS — 확정된 정책·설계·구조 결정 (SSOT)

형식: `YYYY-MM-DD · 요지 · 사유`

2026-09-24 · 재고칸 = 재고현황 있으면 값(0=품절)·없으면 판매중지 무관 "미입고"(옛 판매중지→공란 폐기·`_block_sellable` 제거) · 제한계정 all-SUSPENDED여도 재고칸이 텅 비지 않게(웰빙곳간 47블록중 31공란 실측).
2026-09-24 · 판매상태(productStatus)는 쿠팡 그대로 존중, 상품블록 '판매상태' 지표행(`M_SALE_STATUS`)에 실행일마다 기록(재고칸 아님·보정 안 함) · 담당자 정정용 상시표기(소유자 선택=지표행 방식).
2026-09-24 · 업번들 잔재 블록은 매 수집 시 vid 기준 자동삭제(`_purge_upbundle_blocks`) · 리스팅 전체 업번들로 추적 제외된 상품의 고아 블록 정리(별도 도구는 담당자 변동과 충돌).
2026-09-24 · 3개 데이터 API 응답 **원문(가공 없음)** 을 매 실행 gzip 사이드카(`output/_raw/`)로 상시 보관(`SAVE_RAW_RESPONSES`) · 재수집·재빌드 없이 다양한 오프라인 분석·실측 감사추적(사용자 요청).
2026-09-24 · 중복 대표 판정 실측 결론(_raw 6계정 120그룹): 유령=registrationType NORMAL·실제=RFM 100% · 상품조회에 productId 없음 확정.
2026-09-24 · 죽은 중복 블록 정리는 **안전 sweep**(`_sweep_dead_duplicates`): 같은 상품군에 live 형제 존재 AND 블록 vid 전부 상품조회에서 소멸, **두 조건 모두**일 때만 삭제 · blanket("vid 없으면 삭제")은 판매중지 데이터 유실이라 금지(신규·취소선 미운영·대장삭제·변형·판매자배송twin·상품조회실패 보존) · 그룹키=마스터 블록명 접두(이름 드리프트 무관·live=vid 앵커).
2026-09-24 · 상품 헤더 셀(제목·VID 아래)에 로켓그로스 정보 묶음 표시 — **로켓그로스 판매일**(saleStartedAt 근사·"입고일"에서 명칭변경)·**최근입고 요약**(관리대장, 유형A). vid 파서 'VID :' 줄만 안전 파싱. **판매가는 헤더가 아니라 '판매가' 지표행(재고현황 아래)에 일자별**(마케팅 일환 변동 추적). 판매자배송은 로켓그로스 판매일 줄 생략.
2026-09-24 · **체험단 시작일부터 1개월**간 노출순위(키워드) 행 date칸에 배경색(`_promo_cols`·마케팅 종료일 무관) — 체험단 효과 구간 시각화.
2026-09-24 · **판매자배송(NORMAL) twin 블록은 삭제하지 않고 보존**(소유자 확정) · 로켓그로스+판매자배송 동시 등록 상품의 NORMAL twin 은 코팡에 vid 가 살아있어 죽은 vid 아님 → 판매중지 표기로 유지(데이터 유실 방지) · 완전 제거는 registrationType 배선 필요한 별도 후속(보류).
2026-09-24 · 계정목록 상태 열 = **관리대장 상태만**(소유자 확정) — 대장 판매중지 > 미수집 > 체험단 상태. 쿠팡 상품조회 판매상태(임시저장·승인반려 등)는 날짜별 '판매상태' 지표행에 이미 있어 계정목록에선 뺌(2026-09-22 productStatus 폴백 되돌림·상태는 소스별 위치 분리).
2026-09-24 · 관리대장 로켓그로스 입고 컬럼(요청일자·요청수량·작업수량·박스·파레트·완료일자·출고일자)을 결과파일 헤더 '최근입고' 한 줄로 요약(`_inbound_summary`·유형A) — 담당자 참고용, 역기록 미접촉.
