"""전역 상수·정책값 (SSOT: designs/DESIGN.md)."""
from __future__ import annotations

# ── 셀독 출력 서식(신규) — 시트=사업자, 상품블록(계약/개인)+키워드 노출순위, 일자 가로 ──
SELDOC_SHEET_TITLE = "셀독 상품 데이터"       # 워크북 A1 제목
M_SALES = "판매량"        # 계약(로켓그로스) 상품 일자별 판매량
M_VISITORS = "방문자"     # 계약 방문자수
M_VIEWS = "노출량"        # 계약 노출량(조회수)
M_INVENTORY = "재고현황"   # 판매가능 재고수량 — **로켓그로스(계약)만** 해당(rfm-inventory)
INV_NOT_INBOUND = "미입고"  # 재고현황 API에 없는 판매중 로켓그로스 원상품 = 물류센터 실입고 안 됨(재고 0과 구분)
M_RANK = "노출 순위"      # 키워드별 오가닉 순위(PC)
# 기본 지표 = 판매량·방문자·노출량(계약/개인 공통). 재고현황은 로켓그로스(계약)에만 추가.
CONTRACT_METRICS = (M_SALES, M_VISITORS, M_VIEWS, M_INVENTORY)  # 로켓그로스(재고 포함)
PERSONAL_METRICS = (M_SALES, M_VISITORS, M_VIEWS)              # 판매자배송(재고 없음)
# 상품 구분(배송/판매 방식) — API registration_type 로 자동 판별(RFM=로켓그로스, NORMAL=판매자배송).
KIND_CONTRACT = "로켓그로스"          # 옵션이 전부 RFM(로켓그로스)
KIND_PERSONAL = "판매자배송"          # 옵션에 RFM 없음(판매자배송)
KIND_BOTH = "로켓그로스+판매자배송"   # 한 상품에 RFM 옵션과 NORMAL 옵션이 **섞임**(둘 다 운영)
KINDS_WITH_INVENTORY = (KIND_CONTRACT, KIND_BOTH)   # 재고현황 행·조회 대상(로켓그로스 파트 존재)
# vid 보강 조회 창(일) — 대장 상품인데 **당일(D-1) 판매·방문이 0이라 판매분석에 안 잡힌** 상품의 vid 를
# 최근 N일 판매분석으로 확보한다(vi-detail-search 는 그 기간에 조회/방문/판매가 있은 상품을 반환). ⚠ 이 창
# 조회는 **vid·상품명 roster 전용** — 지표(노출/판매/방문)는 당일 것만 기록한다(넓은기간 합계 오염 금지).
SALES_VID_WINDOW_DAYS = 30
# 상품 이름칸 표시: 1줄=쿠팡 full 상품제목, 2줄=상품 인식코드(vendorItemId). 두 값을 한 셀에 담되
# **보이지 않는 구분자(U+2063 INVISIBLE SEPARATOR)**로 나눈다. 상품 키(=시계열 정체성)는 항상
# 이 구분자 앞부분(=순수 상품명). ⚠️ 상품명 자체에 내부 개행('\n')이 들어있는 경우가 실제로 있어
# (예: '다리스트레칭 MS007\n\n(&picks …)') '첫 줄만 파싱'은 키를 깨뜨린다 → 개행에 안전한 구분자 사용.
# 쿠팡 제목엔 이 제어문자가 절대 없으므로 내부 개행 상품명도 그대로 보존된다. workbook·검증도구 공용.
NAME_ID_SEP = "⁣"

# 모바일 노출순위 포함 여부 — 기본 제외(요청). rank._set_mobile/organic_ranks(mobile=) 모듈은 보존(재사용).
RANK_INCLUDE_MOBILE = False

# (구 서식 지표 상수 제거 — 셀독 새 서식은 위 SELDOC_*/M_*/CONTRACT·PERSONAL_METRICS 사용)
RANK_GOOD_THRESHOLD = 20      # 이 순위 이내면 '양호'(상위 노출), 밖이면 마케팅 필요(diagnose_exposure)

# ── 검색 순위 추적 정책 ─────────────────────────────────────────
RANK_SCAN_MAX = 50           # 오가닉 50위까지 탐색(속도). 밖이면 '{RANK_SCAN_MAX}위 밖'(미노출 아님 — 뒤쪽 순위)
# 반자동 전용 스캔 상한 — 사람이 직접 검색한 '이미 떠 있는 페이지' 하나만 읽으므로 트래픽/차단 부담이 없다.
# 자동(50위 제한)과 달리 로드된 페이지의 오가닉 전부를 세어 **50위를 넘어도 실제 등수를 기록**한다
# (한 페이지 실측 ~60개, 사람이 다음 페이지로 넘기면 그 페이지 기준. 여유 있게 크게 잡음).
RANK_SCAN_MAX_SEMI = 300
# 반자동 자동제출 — True 면 사람이 쓰던 그 창(신뢰 세션)에서 앱이 키워드 자동채움 + **Enter까지 자동**
# 제출해 무인 조회한다(사용자 요청, 손 안 대고 진행). ⚠️ 자동 제출은 goto 자동모드처럼 Akamai 봇탐지·계정
# 플래그 위험이 있어(원래 반자동을 사람 검색으로 둔 이유), **키워드 사이 사람속도 간격(RANK_NAV_DELAY) + 차단
# 감지 시 즉시 중단(연속 실패 RANK_SEMI_AUTO_MAX_MISS회)**로 계정을 태우지 않게 한다. False 면 예전처럼 사람이 Enter.
RANK_SEMI_AUTOSUBMIT = True
# 사람같은 마우스 이동·스크롤·호버를 **실제 브라우저 입력**으로 재현(2026-09-14 소유자 지시 — "최대한 사람처럼").
# 곡선 이동+지터+오버슈트·읽는 리듬 스크롤·상품 호버(클릭 없음). 타이핑과 같은 실제 입력 계열(지문위조 아님).
# ⚠ 실효성 미검증 → 깨끗한 IP(핫스팟) A/B로 판정할 것. 구현=`human_mouse.py`, [[rank-antiblock-circuit-breaker]] 정책변경.
RANK_HUMAN_INTERACT = True
# ⚠️ **검색어 입력 후 실행(Enter/요청)까지 사람같은 멈춤**(초). 붙여넣고 즉시 실행=봇 패턴 → 차단 위험↑
# (사용자 관찰). 대기 = **글자수 × 초당타이핑 + 기본여유**(사람이 타이핑하는 시간 모사, 무작위 아님·사용자 지정).
# 예: 5글자 → 5×1.0 + 2.0 = 7초 (= '글자수 + 2초'). ①②③ 쿠팡 검색어 입력 경로 **공용**(반자동 Enter·자동완성).
TYPE_DWELL_BASE_SEC = 2.0      # 기본 여유(초)
TYPE_DWELL_PER_CHAR_SEC = 1.0  # 글자당 추가(초) — '글자수 + 2초' = 글자당 1초 + 기본 2초


def type_dwell(text) -> float:
    """검색어 입력 후 실행까지 사람같은 멈춤(초) = 글자수 × 초당타이핑 + 기본여유. 무작위 아님(사용자 지정)."""
    return len(str(text or "")) * TYPE_DWELL_PER_CHAR_SEC + TYPE_DWELL_BASE_SEC
RANK_SEMI_AUTO_MAX_MISS = 3   # 자동제출 후 연속 미감지/차단 이 횟수면 '차단 감지'로 보고 쿨다운(아래) 진입
RANK_SEMI_AUTO_WAIT_SEC = 40  # 자동제출 1건의 결과 로딩 최대 대기(초). 넘으면 미감지로 카운트
# 차단 감지 시 **하드 스톱 대신 긴 쿨다운 후 자동 재개**(무인 장시간 운용 — 자리 비운 새 4시간 방치 방지).
# ⚠️ 짧은 백오프로 두드리면 IP만 탐(메모리 안티차단) → **충분히 긴 쿨다운** + **쿨다운 후에도 진전 0이
# RANK_SEMI_COOLDOWN_MAX회 연속이면 그때 당일 중단**(IP 하드플래그로 판단, 무한 재시도 금지).
# 쿨다운 뒤 한 개라도 측정되면(진전) 카운터 리셋하고 계속 → IP가 풀리는 한 밤새 점진 수집.
RANK_SEMI_COOLDOWN_SEC = 1800  # 차단 감지 후 재개까지 대기(초). 30분(사용자 지정 — 볼륨 플래그 완화에 여유·IP 부담↓)
RANK_SEMI_COOLDOWN_MAX = 4     # 쿨다운 후에도 진전 0이 이만큼 연속이면 당일 중단(그 IP는 회복 불가로 판단)
RANK_PAGE_DELAY_MIN = 2.0     # 페이지 간 최소 지연(초). 스캔50=1페이지라 거의 미발동이나 2P+ 시 여유
RANK_PAGE_DELAY_MAX = 5.0     # 페이지 간 최대 지연(초)
# 병렬 fetch 순위조회(기본) — 검색 1회 네비로 Akamai 프라임 후 여러 키워드를 same-origin fetch로 동시에 받아
# 렌더 없이 파싱(실측: 프라임 후 3키워드 병렬 ~1.3초, 현행 네비 15초/키워드 대비 대폭 가속).
RANK_ORGANIC_PER_PAGE = 60   # 검색 1페이지 상품수(실측 60) → 스캔 50은 1페이지로 충분(페이지네이션 불필요)
RANK_HUMAN_SERIAL = True      # (구 병렬 fetch 경로 전용) 직렬 fetch 여부. RANK_NAV_SERIAL=True면 이 경로 안 씀
# 순위 안전 모드 — 공개검색(www.coupang.com)은 최고강도 봇방어. 병렬 fetch 버스트 대신 **사람처럼 검색창을
# 하나씩 치는 직렬 네비게이션 + 긴 간격**으로 조회(위조 아님, 정상 사용 패턴). 차단 감지 시 즉시 중단.
RANK_NAV_SERIAL = True        # True=직렬 네비게이션(기본, 안전). False=구 병렬 fetch(빠르나 봇틱)
# ⚠️ 키워드(검색) 사이 대기 — **자동·반자동 직렬 순위조회 공용**. 실제 대기 = MIN~MAX 무작위(사람처럼).
# 이 한 세트가 **IP 상황과 무관하게 그대로** 쓰인다(상황별 자동 전환 로직 없음 — 값 조정은 **수동 편집**).
# 정책(사용자 지정): **차단 방지 > 속도**("조금 느려도 좋으니 IP차단만은 막는다"). 그래서 기본을 넉넉히 잡는다.
# 수동 교체용 프리셋(위험도↓ 순): 최안전=90~150 · 현재 기본=45~75(아래 값) · 뜨거운/flag된 IP=60~120 · 새 IP(핫스팟)=25~50.
#   ⛔ '급할 때' 초단축(15~35) 프리셋은 차단 위험이 커 **폐기**한다(쓰지 않음 — 속도보다 무차단 우선).
# 순위추적은 KW_MAX_TRACK(=7)개/상품 안쪽이라, 넉넉히 잡아도 무인 야간 운용(서킷브레이커·쿨다운 동반)으로 소화됨.
# 2026-09-14: 밤샘 반자동(271키워드/10.5h·차단 0건) 실측상 대기가 총시간의 ~86% → 90~150(평균120)을
# 45~75(평균60)로 하향(총시간 ~10.5h→~6h). 무차단 유지되면 다음 단계 30~50 검토(단계적 하향 원칙, §위 폐기된
# 초단축과 달리 실측 근거). 설정탭에서 실행 중에도 조정 가능(A/B).
RANK_NAV_DELAY_MIN_SEC = 35   # 최소 대기(초) — 평균 ~45s(45~75서 단계 하향, 소유자 2026-09-23·차단 0 유지 확인 후 유지/추가하향)
RANK_NAV_DELAY_MAX_SEC = 55   # 최대 대기(초). 무작위. ⚠차단 뜨면 45~75로 되돌릴 것
RANK_FETCH_CONCURRENCY = 2   # 병렬(직렬모드 off) 시 동시 fetch 개수. RANK_HUMAN_SERIAL=True면 1로 강제(직렬)
RANK_FETCH_JITTER_MIN_MS = 700  # 각 fetch 전 최소 지연(ms) — 직렬모드 사람 간격 하한(0이면 하한 없음)
RANK_FETCH_JITTER_MS = 1800   # 각 fetch 전 최대 지연(ms) — 실제 지연은 MIN~MAX 무작위(사람처럼). 직렬이라 여전히 빠름(요청당 fetch ~0.3s)
# 차단(Akamai) 감지 시: 즉시 공란 대신 잠시 쉬었다가 재시도한다(플래그가 수십 초~분 내 완화되는 특성 활용).
RANK_BLOCK_BACKOFF_SEC = 30  # (구 병렬 fetch 경로 전용) 차단 감지 후 재시도까지 대기(초)
RANK_BLOCK_RETRIES = 1       # (구 병렬 fetch 경로 전용) 차단 시 백오프 후 재시도 횟수(0이면 즉시 공란)
# 직렬 네비게이션(기본) 차단 대응 = **서킷브레이커(NORMAL→WARNING→COOLDOWN→RECOVERY)**:
# 이상징후(응답시간 급증·403/429·Akamai 챌린지) 감지 시 신규검색 즉시 중지 → 충분한 cooldown →
# 소량 probe(재측정) → 정상이면 재개, 또 이상이면 당일 중지(쉰 시간/IP에 이어서). 우회 재요청 금지.
# (점감 백오프 폐기 — 볼륨 차단은 짧은 대기로 안 풀림. 두드릴수록 IP만 태운다.)
RANK_SLOW_ABS_SEC = 8        # 검색 1건이 이 초 이상 = 이상징후(정상 ~2~4s, 챌린지=셀렉터 타임아웃으로 지연)
RANK_COOLDOWN_SEC = 900      # 이상 감지 시 신규검색 중지 후 대기(충분히 길게·고정). 15분(무차단 우선 상향)
RANK_COOLDOWN_MAX = 2        # cooldown 반복 상한 — 초과 시 당일 중지
# 순위 미처리분(차단 등으로 공란) **자동 재시도(미처리분 보완)** — 전체실행 종료 직전. vid 있는 상품의 공란 키워드만 재측정.
# ⚠️ 차단 뒤 즉시 재시도는 IP만 태운다 → 라운드 사이 충분한 쿨다운(IP flag 완화 시간) + **진전 0이면 즉시 중단**.
# 검색어·로그인 입력 = 사람처럼 한 글자씩 실제 키보드(붙여넣기 금지). 한글은 CDP IME 자모 단위 조합.
# ⚠️ 쿠팡은 붙여넣기(비신뢰 input)를 차단하고 실제 키입력만 통과(실측). True=자모 IME 조합(가장 사람같음),
# False=음절 단위 신뢰 키입력만(폴백). 값 검증 실패 시 음절→URL 순으로 자동 폴백하므로 깨져도 안전.
TYPE_JAMO_IME = True
# 한글 조합 커밋 단위 — "eojeol"=어절 전체를 한 조합으로 커밋 1회(compositionend[F] 빈도↓, 단 조합이 여러 음절에 걸침),
# "syllable"=음절마다 커밋(실제 IME 구조에 가깝지만 compositionend[F]가 음절 수만큼). 탐지 로직 미지 → 핫스팟 A/B 비교용 스위치.
TYPE_JAMO_COMMIT_MODE = "eojeol"
# 타이핑 속도 = **초중급자**(느리고 불규칙). 자모/글자 하나당 간격(초). 붙여넣기처럼 빠르면 봇 티가 남.
TYPE_KEY_DELAY_MIN = 0.14     # 기본 타건 간격 하한(초)
TYPE_KEY_DELAY_MAX = 0.38     # 기본 타건 간격 상한 — 평균 ~0.26s/키(초중급 ~3~4타/초)
TYPE_HESITATE_PROB = 0.22     # 가끔 '망설임'(키 찾기) 확률
TYPE_HESITATE_MIN = 0.25      # 망설임 추가 시간 하한
TYPE_HESITATE_MAX = 0.75      # 망설임 추가 시간 상한
TYPE_LONGPAUSE_PROB = 0.05    # 드물게 '긴 멈춤'(생각) 확률
TYPE_LONGPAUSE_MIN = 0.8      # 긴 멈춤 하한
TYPE_LONGPAUSE_MAX = 1.6      # 긴 멈춤 상한
RANK_BACKFILL = True                 # 전체실행 후 공란 순위 자동 재시도 on/off
RANK_BACKFILL_ROUNDS = 2             # 최대 재시도 라운드 수(진전 없으면 그전에 중단)
RANK_BACKFILL_COOLDOWN_SEC = 1800    # 라운드 간 쿨다운(초). 30분(IP 완화 여유 — 무차단 우선)


# ── 키워드 추천 정책 (황금키워드) ───────────────────────────────
KW_MIN_VOLUME = 500        # '키워드 추천' 탭 전용 하한(밴드 하한). 배치 추적엔 KW_TRACK_MIN_VOLUME 사용
KW_MAX_VOLUME = 50000      # 상한 — '키워드 추천' 탭 전용(추적 선정에는 미적용)
KW_TRACK_MIN_VOLUME = 30   # 배치 추적 하한 — 저경쟁 롱테일(월검색량 수십)이 오히려 노릴 값이라 낮게. 0~극소량만 제외
KW_GEN_N = 20              # AI가 조합 생성할 고객검색어 후보 수(핵심×속성×형태×장소×용도). 토큰 절감 위해 40→20
KW_JUDGE_POOL_N = 30       # AI 판정(judge) 전 후보 축소 상한 — 검색량 상위 이만큼만 판정(상품 자기 정체성은 항상 포함). 토큰 절감
KW_EXPAND2_N = 6           # 네이버 2단계 확장: 1차 후보 중 검색량 상위 몇 개를 재시드로 다시 연관조회할지(요청 폭주 방지 상한)
KW_CANDIDATE_LIMIT = 15    # 쿠팡 1P 경쟁까지 수집할 후보 수 (요청 수 제한)
KW_TOP_N = 8               # 추천 상위 개수 (사용자가 이 중 3~5개 확정)
KW_TRACK_N = 4             # 첫날(새 통계) 상품당 자동 추적할 키워드 수(AI 최종선정 개수)

# ── 통계 유지(cross-day) — 키워드 동결 + 상한 내 발굴 추가 ──────
# 키워드 = **결과 구글시트 셀 값이 기준**(담당자가 넣은 그대로 사용·삭제 시 이력째 제거, 소유자 2026-09-20).
# 상품당 추적 키워드 상한 = KW_MAX_TRACK(가변). AI 발굴·담당자 수동 추가 공통 상한.
KW_MAX_TRACK = 10          # 상품당 추적 키워드 상한(가변·config에서 조정)
KW_ADD_PER_DAY = 2         # '새 키워드 발굴 추가' 켠 날, 상품당 하루 최대 추가 개수(상한 KW_MAX_TRACK 초과 금지)

# ── Keyword Score(페이즈 B — 선정 지능화) ──────────────────────
# 후보를 부분점수(노출 제외)로 압축 → 압축분만 쿠팡 실노출 측정 → 전체 95점·등급 → AI가 종합해 최종선정.
KW_SCORE_POOL_N = 6        # 부분점수 상위 몇 개만 쿠팡 실노출 측정(요청량·IP차단 제어, 6~7 권장)
KW_SCORE_W_RELEVANCE = 35  # 관련성: AI 핵심/연관 판정(핵심=만점, 연관=55%)
KW_SCORE_W_INTENT = 25     # 구매의도: 월클릭수(로그)72% + 롱테일성(키워드 길이)28%
KW_SCORE_W_VOLUME = 20     # 검색량: log10(월검색량) 정규화
KW_SCORE_W_EXPOSURE = 15   # 쿠팡 실노출: 오가닉 최상위 순위(1~3=만점, 미노출=0)
KW_GRADE_A = 72            # 이 점수 이상 A(총점 95 만점 기준)
KW_GRADE_B = 54            # B
KW_GRADE_C = 36            # C, 미만 D

KW_W_VOLUME = 1.0          # 검색량 가중(클수록 좋음)
KW_W_ROCKET = 1.5          # 로켓비율 패널티(높을수록 판매자배송 진입 어려움)
KW_W_AD = 0.5              # 광고수 패널티(경쟁 과열)
# ⚠️ '키워드 추천' 탭의 쿠팡 1P 경쟁수집(page1_competition)은 **순위조회와 같은 쿠팡 검색(Akamai 면)** 을 때린다.
# 최대 KW_CANDIDATE_LIMIT(=15)회 연속이라 짧은 간격이면 버스트 → 그 IP가 flag돼 직후 순위조회까지 막힌다.
# 차단 방지 우선(사용자 지정)으로 넉넉히 잡는다. (배치 ② 노출측정은 measure_ranks=직렬 경로라 이미 RANK_NAV_DELAY로 안전.)
KW_QUERY_DELAY_MIN = 8.0   # 키워드 간 쿠팡 조회 지연(초) — 순위와 같은 엔드포인트라 여유있게
KW_QUERY_DELAY_MAX = 16.0

# ── AI 키워드 (OpenAI ChatGPT) — 앵커 추출 + 관련성·쇼핑성 판정 ──
KW_AI_MODEL = "gpt-4o"         # 키워드 AI 전 단계 공통. 설정 탭에서 변경 가능(mini는 judge 속도 이득 없어 미사용)
KW_AI_ANCHOR_N = 4              # 제목에서 뽑을 핵심 앵커 최대 개수(다중 허용)
KW_JUDGE_BATCH = 20            # 판정 1회 최대 후보 수 — 작게 나눠 **배치 병렬**로 처리(judge 30s→~10s)

# ── 세션 관측(observability) — 추측 대신 실측용. 비밀값(비번·쿠키·토큰) 저장 금지, 재생성 가능 ──
SESSION_STATE_DB = "data/session_state.db"   # 계정별 세션 상태 + 이벤트 로그(평문 SQLite, 조언용)

# ── 로그인 정책값 ──────────────────────────────────────────────
# 로그인 서킷브레이커: 자동로그인이 Akamai에 연속 이 횟수만큼 차단되면 이후 계정은 로그인 생략(IP 그만 태움).
# 세션우선 2패스와 함께: 세션 살아있는 계정 먼저 다 수집 → 로그인 필요분만 시도하다 연속 차단 시 중단.
LOGIN_BLOCK_CIRCUIT = 3
# 로그인 페이싱: 전원off 후 아침 재로그인 시 로그인을 몰아치면 IP 플래그(과거 ~14회째 차단). 사람 간격으로
# 분산해 속도/평판 신호를 낮춘다(탐지회피 아니라 정상 부하제어). 로그인 사이에만 적용(첫 로그인 제외).
LOGIN_PACE_MIN_SEC = 45      # 로그인 간 최소 대기(초) — 로그인도 Akamai 면(과거 버스트로 ~14회째 차단), 여유 상향
LOGIN_PACE_MAX_SEC = 120     # 로그인 간 최대 대기(초) — 실제는 MIN~MAX 무작위
# 무인(unattended) 로그인 — True 면 자동입력·자동제출만으로 되는 계정을 손 안 대고 수집하고,
# **사람 개입이 필요한 상태(Akamai 차단·2차인증·폼 지속=봇차단)는 창을 띄우지 않고 즉시 건너뛴다**
# (자리 비운 채 창이 떠서 방치되는 것 방지). 건너뛴 계정은 공란 → 나중에 사람이 반자동/수동으로 수집.
# 정책 준수: 지문위조·우회 없음(그냥 사람 필요분을 미룰 뿐). 연속 차단은 LOGIN_BLOCK_CIRCUIT로 중단.
LOGIN_UNATTENDED = True
LOGIN_UNATTENDED_WAIT_SEC = 45   # 무인 시 계정당 로그인 확인 최대 대기(초). 자동성공/실패를 빨리 판정, 넘으면 건너뜀
LOGIN_BLOCK_GRACE_SEC = 8        # 무인 시 Akamai 차단 감지 후 이 초만 보고 건너뜀(사람 없으니 오래 안 기다림)
# ① 무인 로그인이 차단/폼정체로 실패하면 **창을 띄워 앱이 사람처럼 자동입력·클릭으로 딱 1회 더** 시도(사용자 선택).
# ⚠️ 제출이 1회 추가되므로(낯선 환경 5회 오류=계정잠금·위탁계정) **계정당·실행당 정확히 1회**로 엄격 제한한다.
# Akamai IP 접근차단은 이걸로도 대부분 못 뚫는다(지문위조 금지) — 소프트 차단(폼 되돌아옴)에만 효과 기대.
LOGIN_SEMI_ON_BLOCK = True
LOGIN_SEMI_WAIT_SEC = 90         # 그 1회 재시도의 로그인 완료 최대 대기(초, 창 표시 상태)
# 무인 야간(--auto): ① 실행 후 차단 등으로 미완료 계정이 남으면 **쿨다운 후 남은 계정만 1회 재개**.
# ⚠️ 제출 총량을 억제(위탁계정 잠금 방지)하려 **밤에 딱 1회만** 재개한다(무한 재시도 금지). 낮 재실행이나
# 서킷브레이커와 별개 — Akamai IP 차단이 쿨다운 사이 풀리면 그때 남은 계정을 건지는 목적.
LOGIN_NIGHT_RESUME = True
LOGIN_NIGHT_RESUME_COOLDOWN_SEC = 1800   # 재개 전 쿨다운(초). 30분(사용자 지정)

# ── 출력 파일 ──────────────────────────────────────────────────
OUTPUT_FILE_PREFIX = "쿠팡데이타분석"   # {prefix}_통계.xlsx / _통계_yymmdd.xlsx(스냅샷)

# ── UI 상시가동(24/365) 안정성 ─────────────────────────────────
# 로그 콘솔은 최근 N줄만 유지(오래된 줄 폐기) → QTextEdit 무한 증가로 인한 메모리 잠식 방지.
# 전체 이력은 파일 미러(run_log_*.log)에 남는다.
UI_LOG_MAX_LINES = 5000
# 로그 파일이 이 크기를 넘으면 .1 로 회전(직전 1세대 보관) → 디스크 무한 증가 방지.
UI_LOG_FILE_MAX_BYTES = 20_000_000   # 20MB

# ── 입력 분석용 엑셀 헤더 ────────────────────────────────────────
# **가져오는 값 = 사업자명·계정아이디·상품명(필수 3개).** 대표자명은 선택(빈 사업자명 시트 라벨 폴백용).
# (비밀번호는 별도 경로=parse_password_file→DPAPI 암호화. 옵션/vendorItemId/productId 는 미파싱 — vid 는
#  라이브 판매수집에서 확보하므로 입력에서 안 가져온다. 2026-09-12 입력 정리.)
IN_COL_REPRESENTATIVE = "대표자명"   # 선택
IN_COL_BUSINESS = "사업자명"
IN_COL_ACCOUNT_ID = "계정아이디"
IN_COL_PRODUCT = "상품명"
# 마케팅 기간(선택) — 관리대장 상품 행에 있으면 파싱해 상품별 수집주기·비고·배경색 구동. 헤더 별칭(정규화=소문자·공백제거).
IN_ALIASES_MKT_START = ("마케팅시작일", "마케팅시작", "mktstart")
IN_ALIASES_MKT_END = ("마케팅종료일", "마케팅종료", "mktend")
IN_ALIASES_MKT_MON = ("모니터링종료일", "모니터링종료", "monitorend", "monend")
# 상태 컬럼(선택) — 삭제/판매중지 감지용. **구글시트(Sheets API) 읽기는 취소선을 못 읽으므로**
# 파일의 취소선 대신 이 '상태' 컬럼 값으로 계정/상품의 판매중지·삭제를 감지한다(사용자 지정, 2026-09-13).
# 별칭은 정규화(소문자·공백제거) 형태로 적는다. 상태값 마커(아래)가 셀에 포함되면 그 행(계정/상품)을 제외.
IN_ALIASES_STATUS = ("상태", "status", "판매상태", "진행상태", "계정상태", "상품상태")
# 판매중지/삭제로 볼 상태값(부분일치). '판매부진'은 계속 판매라 제외 마커에 넣지 않는다(기존 원칙 유지).
IN_STATUS_DISCONTINUED = ("판매중지", "판매중단", "판매종료", "판매불가", "중지", "중단",
                          "종료", "삭제", "해지", "제외", "비활성", "미진행")

# ── 상품별 판매 리포트 헤더 (필수 매칭 대상) ────────────────────
RP_COL_PRODUCT = "상품명"
RP_COL_ITEM_ID = "등록상품ID"
RP_COL_OPTION_ID = "옵션 ID"
RP_COL_VIEWS = "조회"        # → 노출건수
RP_COL_SALES = "판매량"      # → 판매건수
RP_COL_VISITORS = "방문자"   # → 방문자건수


# ── 표준 로그 포맷 (모든 로그 싱크 공통: GUI·CLI 도구) ─────────────
#   표준 = "[YYYY-MM-DD HH:MM:SS.mmm] 메시지"
#   - 날짜+시분초+밀리초 → 정렬 가능·grep 가능·단계별 소요시간(ms) 분석 가능.
#   - 메시지 본문 규칙(별도): 상품/옵션 단위 로그는 vid= 태그를 넣어 grep vid=<값> 으로 추적.
import datetime as _datetime


def log_ts() -> str:
    """표준 로그 타임스탬프 문자열 'YYYY-MM-DD HH:MM:SS.mmm'."""
    n = _datetime.datetime.now()
    return f"{n:%Y-%m-%d %H:%M:%S}.{n.microsecond // 1000:03d}"


def format_log(msg: str) -> str:
    """표준 로그 한 줄 = '[타임스탬프] 메시지'. 모든 로그 싱크가 이 함수로 한 줄을 만든다."""
    return f"[{log_ts()}] {msg}"
