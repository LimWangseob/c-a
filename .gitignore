# 세션·자격·출력물·캡처 — 절대 커밋 금지
/data/
/capture/
/output/
# 설정 파일(보존 폴더·비밀 아님이지만 PC별 링크라 커밋 안 함 — 앱이 프로젝트 루트에 자동 생성)
/config.json
*.har
creds.json
.env
# 배포 패키지에 담기는 평문 설정(API/SA 키 포함) — 절대 커밋 금지(설치 시 자동 삭제되는 임시 파일)
_설정값.json
설정값.json

# 로그인 폼 소스 캡처(봇탐지 관련·민감) · 로컬 백업 복사본 — 커밋 금지
/login_source_*/
/login_source_*.zip
/src - 복사본/

# PyInstaller 빌드 산출물 — 커밋 금지(build.bat 로 재생성)
/build/
/dist/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# OS
.DS_Store
Thumbs.db
