# 설치·배포 안내 (터미널 없이 다른 PC에서 실행)

이 프로그램은 이제 **더블클릭으로 실행되는 단독 실행파일(.exe)** 로 배포할 수 있습니다.
사용자는 파이썬·터미널이 필요 없습니다. 딱 하나, **실행하는 PC에 Google Chrome이 설치**돼 있어야 합니다
(정책상 실제 Chrome으로만 동작합니다).

---

## A. 빌드하는 사람 (개발 PC에서 1회, 파이썬 필요)

1. 개발 PC(파이썬 설치된 지금 이 PC)에서 프로젝트 폴더의 **`build.bat` 더블클릭**.
   - 필요한 패키지와 PyInstaller를 자동 설치하고 EXE를 만듭니다(수 분 소요).
2. 완료되면 결과물:
   ```
   dist\쿠팡애널리틱스\쿠팡애널리틱스.exe   (+ 같은 폴더의 _internal 등 부속 파일)
   ```
3. **`dist\쿠팡애널리틱스` 폴더 전체**를 압축하거나 USB/공유로 다른 PC에 전달합니다.
   (exe 하나만 복사하면 안 됩니다 — 같은 폴더의 부속 파일이 함께 있어야 실행됩니다.)

> 터미널로 직접 빌드하려면: `python -m PyInstaller --noconfirm coupang_analytics.spec`

---

## B. 사용하는 사람 (다른 PC, 파이썬·터미널 불필요)

1. **Google Chrome 설치** — 없으면 https://www.google.com/chrome 에서 설치.
   (안 깔려 있으면 실행 시 안내창이 뜨고 종료됩니다.)
2. 받은 **`쿠팡애널리틱스` 폴더**를 **쓰기 가능한 위치**에 둡니다.
   - 권장: 바탕화면, 문서 폴더 등.
   - ❌ 금지: `C:\Program Files` 같은 보호 폴더(산출물 저장이 막힙니다).
3. 폴더 안의 **`쿠팡애널리틱스.exe` 더블클릭** → 프로그램 창이 뜹니다.
4. 처음 1회 **설정 탭**에서:
   - 입력 엑셀(관리 대장) 열기
   - 네이버 검색광고 API 키 / OpenAI(ChatGPT) API 키 입력
   - (비밀번호는 입력 엑셀의 '비밀번호' 컬럼에서 자동 저장)
   - ⚠ **API 키·비밀번호는 이 PC 전용으로 암호화 저장(DPAPI)** 됩니다. 다른 PC로 옮기면 **다시 입력**해야 합니다.
     (다른 PC에서 암호화한 값은 복호화되지 않습니다 — 보안상 정상.)
5. 결과 파일(`쿠팡데이타분석_통계.xlsx` 등)과 로그·크롬 프로필은 **exe 폴더 안에 생성**됩니다
   (`output\`, `data\`).

---

## 참고 / 문제 해결
- **로그인/2차인증**: 낯선 환경(사무실 밖·새 PC)에서는 쿠팡이 2차인증을 요구할 수 있습니다. 그때는 뜨는
  Chrome 창에서 직접 인증하세요(위치 기반 정책).
- **백신 경고**: PyInstaller로 만든 exe는 일부 백신이 오탐할 수 있습니다. 사내 배포면 예외 등록하세요.
- **실행이 안 될 때**: exe와 같은 폴더의 `_internal` 폴더가 함께 있는지, Chrome이 설치됐는지, 폴더가
  쓰기 가능한 위치인지 확인하세요.
- **24시간 상시 가동**: 그대로 켜 두어도 됩니다(로그·메모리·크롬 좀비 누적 방지가 반영돼 있습니다).
